# 학원실록 — 작업 컨텍스트

openedu4u.com 에서 도는 학원 정보 서비스. 이 파일은 **반복해서 틀렸던 것들**과
그 해결 방식을 남긴다. 구조 설명은 README.md, 데이터 출처는 docs/DATA.md.

---

## ⚠️ 학원 중복 문제 — 세 종류가 있고, 원인이 전부 다르다

"학원이 중복으로 보인다"는 증상 하나에 원인이 셋이다. 새 중복 신고를 받으면
**어느 종류인지부터 판별**해야 한다. 엉뚱한 곳을 고치면 증상이 안 없어진다.

### 1) 식별자 충돌 — 서로 다른 학원이 같은 id 를 가짐

- **증상**: 같은 학원이 두 학군 랭킹에 동시에 1위. 표본 수가 비정상적으로 큼.
- **원인**: 정규화한 학원명을 id 로 씀. '목동 청담어학원'과 '잠실 청담어학원'이
  모두 `청담` 이 되어 언급과 점수를 공유했다(표본 1,028건짜리 유령).
- **해결**: NEIS 학원지정번호(`ACA_ASNUM`)를 id 로 쓴다.
  4개 구 9,324건에서 결측 없이 고유하다. → `neis.normalize()`
- **재발 점검**: `audit_uniqueness()` 가 id 중복을 0건으로 확인한다.

### 2) 등록 분할 — 한 학원이 여러 건으로 등록됨

- **증상**: 'CMS 대치'가 목록에 5번. 언급이 쪼개져 전부 표본 부족이 됨.
- **원인**: NEIS 는 '관'과 '과정' 단위로 따로 등록한다. 학부모가 하나로 아는
  곳이 영재1관·영재3관·영재4관·중등1관·중등2관 다섯 건이고 주소는 전부 같다.
- **해결**: `dedupe.py` — **같은 주소 + 이름의 브랜드 접두사 일치**로 묶는다.
  - 주소만으로 묶으면 안 된다. 목동서로 349 한 건물에 학원이 78곳 있다.
  - 이름만으로도 안 된다. 지점이 다르면 다른 학원이다.
  - 통합 시 정원은 합산, 교습비는 중앙값, 과목·단계는 합집합, 개설일은 최소.
- **여기서 세 번 틀렸다**:
  1. *3글자 겹치면 같다* → 예설라센트럴원·파피루스문해원·기파랑문해원이
     한 학원이 됐다. **'센트럴'은 건물 이름**이었다. → 접두사 일치로 변경 +
     건물명(센트럴·프라임·타워…)을 불용어에 추가.
  2. *긴 접미사부터 제거* → '뉴클리**어학원**'이 '뉴클리'가 되어 옆 건물
     '뉴클리온'과 묶였다. → **짧은 접미사부터, 끝에서만** 제거.
  3. *과목명까지 제거* → '목동깡**수학과학**2관'이 '깡' 한 글자가 되어 6·7·10관과
     안 이어졌다. → 과목명을 남기는 **약한 정리**를 병행(2단 토큰).

### 3) 표시명 충돌 — 서로 다른 지점이 화면에 같은 이름으로 나옴

- **증상**: 테크트리 > 목동 > 수학에 'CMS영재교육'이 2개.
- **원인**: 카드가 `brand ?? name` 을 찍었다. 큐레이션 브랜드명은 지점을
  구분하지 못한다. 목동서로 133 과 목동서로 349 의 CMS 두 건물이 똑같이
  'CMS영재교육' 으로 나왔다. (1·2번을 고쳐도 이건 안 없어진다)
  — 2026-08-26 정정: 그때는 이 둘을 **다른 지점**으로 적어 두었으나,
  관(館) 통합을 고치며 다시 보니 한 학원의 두 건물이다(이름이 전부
  '목동씨엠에스…관' 꼴이고 전화번호 02-2647-1881·02-2651-0507 이 두
  주소에 걸쳐 겹친다). 지금은 한 곳으로 묶인다.
- **해결**: 표시명의 뿌리는 **항상 NEIS 실제 학원명**. 브랜드명은 표시에 쓰지
  않고 상세의 '브랜드' 행에만 둔다. 같은 이름이 한 학군에 여럿이면
  도로명을 붙인다 → `씨앤씨 (목동서로 389)`. → `build.assign_display_names()`
- **주의**: 표시명은 **채점 대상과 등록부를 한꺼번에 놓고** 정해야 한다.
  따로 정하면 두 목록 사이에 같은 이름이 남는다.

### 중복 신고를 받으면 이 순서로

```bash
python3 pipeline/run.py --from-cache     # 전수 점검 결과가 출력된다
```

`전수 점검: N곳 모두 고유 ✓` 가 나오는지 본다. 안 나오면 출력된 중복 목록을
보고 위 1/2/3 중 어느 종류인지 판별한다. 화면에만 보이고 점검은 통과한다면
**3번(표시 계층)** 이므로 파이프라인이 아니라 위젯을 봐야 한다.

**반대 신고('두 학원이 하나로 합쳐졌다')도 있다.** 이쪽이 더 나쁘다 —
남의 후기가 내 점수가 되기 때문인데, 전수 점검은 통과하므로 안 보인다.
회귀 시험은 `pipeline/tests/test_dedupe.py` 에 있다(묶여야 하는 짝과
묶이면 안 되는 짝을 함께 고정해 뒀다). 실측 예: 정상어학원 ≠ 정상수학학원.

---

## 같은 신고가 또 들어오면 — 셋 중 하나다

'대치정상수학학원' 건에서 사용자가 **같은 신고를 두 번** 했다. 코드는
이미 고쳐져 있었는데도 화면은 그대로였다. 원인이 셋이었고 전부 다르다.
새 신고를 받으면 고치기 전에 **어느 쪽인지부터** 가른다.

1. **고쳤는데 데이터가 안 만들어졌다.** 코드 수정과 데이터 반영은 **다른
   사건**이다. 야간 워크플로는 기본 브랜치에서 돌고, 수정이 main 에 들어온
   시각이 마지막 데이터 갱신보다 **늦으면** 한 회차를 통째로 기다린다.
   실측: 수정 병합 08-30 10:26, 마지막 갱신 08-30 06:58 — 3시간 반 차이로
   다음 날까지 옛 데이터가 서 있었다.
   → 신고를 닫기 전에 `git log -- app/assets/data` 로 **데이터 커밋이 수정
     뒤인지** 확인할 것. 아니면 사용자에게는 아무것도 안 고친 것과 같다.

2. **증상을 고치고 원인을 안 고쳤다.** 신고는 '수학·영어를 결합했는데
   이름이 수학학원' 이었는데, 앞 회차는 **이름만** 바꿨다(수학학원 →
   어학원). 결합은 그대로여서 영어 후기 330건이 여전히 한 엔티티에 있었다.
   → 신고 문장에서 **명사를 고를 것**. '이름' 이 아니라 '결합' 이 주어다.
     증상을 고치면 신고가 다시 온다 — 그때 온 것이 같은 신고인지 새
     신고인지 구분이 안 되는 것이 진짜 비용이다.

3. **시스템이 이미 알리고 있었는데 다른 이름으로 분류돼 있었다.**

       ! 위키: 1000035161: 위키 name '대치1관정상어학원' ≠ 등록명 '대치정상수학학원'

   매 실행 찍히고 있었다. 이것이 곧 '서로 다른 두 학원이 한 id 에 있다' 는
   증거인데, 문구가 '페이지 이관 필요?' 계열이라 **장부 정리 문제로 읽혀**
   여러 회차를 지나쳤다.
   → **이름 불일치 경고는 통합 오류의 신호다.** 그리고 경고 문구는 무엇을
     해야 하는지까지 말해야 한다 — 'N건 있음' 은 아무도 안 읽는다.

---

## 헤더가 잘릴 때 — 임계값을 눈으로 정하지 않는다

"모바일에서 메뉴가 다 안 보인다"는 신고를 받았다. 원인은 하나가 아니라 셋이었고
전부 배치 계산 문제였다.

1. **임계값이 서로를 몰랐다.** 로고 720, 내비 860, 필터 1180 — 각자 정해 놓으니
   900px 에서 내비가 켜지는데 자리가 없어 '산식'과 검색이 잘렸고, 375px 에서는
   학교급 선택기가 화면 밖으로 밀려났다. Row 에 넘침 대책이 없어 그냥 사라졌다.
   → `HeaderLayout.forWidth()` 가 요소별 최소 점유 폭을 더해 들어갈 것만 켠다.
     상수는 전부 실측치다. 눈으로 정한 임계값은 다음 문구 변경에서 또 깨진다.

2. **`Spacer()` 와 `Flexible()` 을 나란히 두면 남는 폭을 반씩 나눠 갖는다.**
   둘 다 flex 자식이기 때문이다. 메뉴는 필요한 만큼의 절반만 받고 왼쪽 두 개가
   밀려 사라졌다. **자리가 없어서가 아니라 안 준 것이었다.**
   → 남는 폭을 다 줘야 하는 쪽은 `Expanded` 하나로 받고 그 안에서 정렬한다.

3. **우선순위를 잘못 두면 잘리지 않아도 못 쓰게 된다.** 폭에 맞춰 끄도록 고쳤더니
   375px 에서 학군·학교급 필터가 통째로 빠지고 '운영사 표기'만 남았다.
   → 필터는 절대 빠지지 않는다. 모양만 접는다(휠 → 한 덩어리 버튼 + 시트).
     **없앨 것은 기능이 아니라 장식이다.**

문구가 길어지면 폭 상수도 같이 고쳐야 한다. '초등' 이 '예비초~초3' 이 되면서
드롭다운이 휠보다 넓어졌고, 좁아질 때 더 넓은 것으로 물러나는 꼴이 됐다.

## 학교급(school level)과 학년 구간(grade band)은 다른 축이다

- **학교**: 초등학교·중학교·고등학교. 지도·학구·배정에서 쓴다. 실제 건물이다.
- **학원**: `grade_band` — 예비초~초3 / 초4~초6 / 중등 / 고등.
  '몇 학년을 받는 학원인가'다. 학원가에서 초등 저·고학년은 사실상 다른 시장이라
  (저학년=사고력·연산·파닉스, 고학년=경시·선행) 한 덩어리로 두면 안 된다.

이름이 `school_level` 로 겹쳐 있어 매번 헷갈렸다. 지금은 갈라 놨으니 섞지 말 것.
예비초는 학년 코드 **0**. 테크트리 트랙은 단계의 학년 범위를 보고 자동으로 갈린다
(`config.banded_techtree()`), 경계에 걸친 단계는 **양쪽에 모두** 넣는다 — 한쪽에서
빼면 길이 끊긴다. 구간 밖 출발점은 `inbound` 로 남겨야 실제 경로가 안 사라진다.
**트랙을 가르면 요약도 같이 갈라야 한다.** 부모 요약을 물려주면 '예비초~초3' 트랙이
그 구간에 없는 경시·선행을 약속하게 된다.

## 첫 화면이 느릴 때 — 무엇을 언제 읽는지부터 본다

정적 번들 4.5MB 를 첫 그림 전에 메인에서 파싱하고 있었는데, 3MB 는 지도와 검색
에서만 쓰는 것이었다.

- 지도 데이터(학교·아파트·학구) → `mapDataProvider`, 지도에 들어갈 때
- 등록부 → `registryProvider`, 검색을 열 때. 도착 전에도 채점 대상은 먼저 보여준다
- 학군별 학원 수는 파이프라인이 미리 세어 `regions.json` 에 넣는다.
  숫자 넷 때문에 1.4MB 를 읽고 있었다

**리버팟 상태 클래스에는 `==`/`hashCode` 를 반드시 넣는다.** 없으면 값이 같아도
매번 '바뀐 것'으로 보고 구독자를 전부 다시 그린다. 휠을 한 번 굴릴 때마다 화면
전체가 재구성되던 원인이었다. 휠은 지나가는 칸마다가 아니라 **멈춘 뒤** 확정한다.

## 목록은 10곳을 채운다 — 세 층으로, 각각 다른 말로

'지역 × 과목 × 학년마다 최소 10곳'이라는 요구를 받고 재 보니 96개 조합 중
순위가 10곳 이상인 곳이 **하나**였다. 문턱만 낮춰서는 안 됐다 — 채점 대상
400곳 안에 아예 학원이 서넛뿐인 조합이 58개였다(대치 과학 예비초 3곳).

세 층으로 채운다. **층마다 붙는 말이 다르다.**

    1. 순위      표본 1건 이상. 점수순 · 등수 있음
                 표본 10건 미만이면 카드에 '표본 부족 · N건'
    2. 미수집    채점 대상인데 표본 0. 등수 없음 · '근거 없음'
    3. 등록부    수집한 적 없음. 등수·점수 없음 · '등록부 · 미수집'
                 (registryFill, 늦게 오는 provider 라 없으면 없는 셈)

결과 94/96. 못 채우는 둘(잠실 과학 중등 9곳·고등 8곳)은 **송파에 등록된
과학학원이 실제로 그것뿐**이다. 없는 것을 만들어 채우지 않는다.

**★ 층을 섞지 말 것.** 등수를 붙일 수 있는 것은 1층뿐이다. 2·3층에 등수를
주면 코호트 평균(50)이나 정원 순서가 화면에서 '평가 결과'로 읽힌다.

## 랭킹에 없는 학원 — '낮은 점수'가 아니라 '보지도 않음'일 때

"내가 아는 대치 저학년 영어 top3가 없다"는 신고를 받았다. 점수 문제가 아니라
**수집 대상 선정** 문제였고, 원인이 둘이었다.

1. **정원(수용인원)으로 한 줄 세우면 업종이 편향된다.** 대치 100칸의 윗자리를
   러셀 자습전용관(정원 54,361)·대찬·시대인재 재종반이 가져갔다. 재종반·자습실형은
   수용인원이 수만이고 저학년 어학원은 72~158명이다. **규모의 대리 지표로 보였지만
   실은 학년대 필터였다.** → 학년 구간별로 자리를 먼저 떼고, 그 안에서 과목을
   번갈아 고른다. 정원 비교는 같은 구간·같은 과목끼리만.

2. **브랜드 매칭이 완전일치였다.** 시드 `청담어학원`→키 `청담`,
   NEIS `대치청담어학학원`→키 `청담어학`. 4,170곳 중 48곳만 이어졌다.
   → 지역·관 번호를 벗긴 **접두** 매칭. 단 확신을 둘로 나눈다:
   - `strict`(업종어까지 떼면 브랜드만 남음) 일 때만 브랜드로 **단정**하고 단계를 붙인다
   - `loose`(`폴리매그넷`·`폴리박사`) 는 단정하지 않고 **수집 우선순위로만** 쓴다

   접두는 **맨 앞에서만** 인정한다. 아무 데나 들어 있으면 같다고 치면
   `뮤엠영어**폴리**오국어논술` 이 폴리어학원이 된다. 그래도 `청담동수학학원` 같은
   동 이름 충돌이 남아 **과목이 어긋나면 버린다.**

**증상이 '순위가 이상하다'여도 먼저 물을 것: 그 학원이 애초에 수집 대상이었나.**
`registry.json` 에만 있으면 점수를 매긴 적이 없다는 뜻이다.

## 공공 API 가 담지 않는 것 — 아파트

`data.go.kr` 공동주택(AptListService3)은 **의무관리대상**만 준다. 대체로 300세대
이상(또는 승강기 있는 150세대 이상)이라, 소규모 단지는 조회 자체가 불가능하다.
'대치 푸르지오 발라드'(78세대)가 그래서 없었다. 페이지네이션 문제가 아니다.

→ `pipeline/data/apartments_extra.yaml` 에 손으로 적는다. 주소만 넣으면 좌표는
지오코딩이, 학교 배정은 폴리곤이 채운다. 네이버 지오코딩은 **주소만** 받는다
(POI 이름으로는 안 찾아진다). 지역검색 API 는 이 키에 미승인이다.

## 중·고 배정 — 추첨이지만 제비뽑기는 아니다

학교군 안 추첨이 맞지만 **통학 편의가 반영**되므로 가까운 학교로 갈 확률이 높다.
학부모도 '추첨이지만 보통 저기 간다'로 안다. 숨기면 화면이 현실보다 덜 알려 주고,
단정하면 거짓이다. → **거리순과 거리값만** 보여준다. 확률(%)로 바꾸지 않는다 —
실제 배정 결과 자료가 없는 상태에서 퍼센트를 붙이면 근거 없는 숫자다.

## 비율 지표는 분모를 먼저 정한다

긍정률의 분모를 '유효 후기 전체'로 잡았더니 전 학원이 25~40%에 몰려 변별이 안 됐다.
국내 커뮤니티 글은 정보 전달 위주의 **중립 서술이 많다.** 의견을 낸 글만 분모에
넣으니 55~100%(중앙 91%)로 벌어졌다. 다른 서비스의 '추천율'(설문 기반)과는
다른 값이므로 산식 페이지에 그렇게 적어 두었다.

## 학교알리미 진로 공시 — API 엔 없고, 웹은 있고, 급하면 차단된다

- OpenAPI apiType 1~70 전수 확인: 진로현황 없음(51은 졸업자 수뿐).
- 웹 공시는 있다: 학교 상세 = `POST /ei/ss/Pneiss_b01_s0.do` + `SHL_IDF_CD`(uuid).
  **이 uuid 는 OpenAPI apiType 0 응답에 들어 있다** — 두 체계의 유일한 연결고리.
  진로 항목은 `POST /ei/pp/Pneipp_b06_s0p.do` (GS_HANGMOK_CD=06, 응답 EUC-KR).
- **연속 호출 몇 번에 '서비스 일시 중단 안내'로 차단**됐다. 2초 간격 엄수.
  → `pipeline/edutree/careers.py`, `run.py --careers` (캐시 이어받기 가능).
- **해결(2026-08-23)**. 세 가지가 겹쳐 있었다.
  1. **요청이 2단계다.** b06 은 껍데기(지표 폼)만 준다. 실제 표는
     `POST /ei/ss/pneiss_a03_s0p.do` 에 그 폼의 JIPYO_CD/JIPYO_COLUMN 을
     그대로 실어야 나온다.
  2. **연도.** 진로 현황은 최신 공시(2026)에 항목이 아예 없다(45개 목록).
     직전 연도(2025)에 있다(48개). 최신으로 부르면 '없음'이 아니라 항목이 없다.
  3. **파라미터를 추측하면 틀린다.** 화면의 `loadGongSi` 인자를 그대로 쓴다 —
     GS_BURYU_CD=JG040, JG_BURYU_CD=JG130, JG_HANGMOK_CD=52, JG_GUBUN=1, JG_CHASU=4.
- **차단 페이지가 '빈 데이터'처럼 보인다.** 길이 약 3.8KB, 본문 없음.
  이걸 못 알아채고 157곳을 전부 '데이터 없음'으로 저장한 적이 있다.
  → 길이(<4.2KB)와 마커를 함께 본다. 6초 간격, 차단되면 즉시 중단·이어받기.
- 값은 인원이 아니라 **비율(%)** 이다.
- 서울대 진학자 수는 웹 공시에도 없다. 출처 있는 보도값만
  `data/school_outcomes_extra.yaml` 에 수기 등재한다. **빈 상태가 정상이다.**

## 유명 학원이 등록부에 아예 없을 때 — 동 경계부터 의심한다

'에디센이 없다'는 신고. NEIS 원본(9,324건)에는 있었고, 법정동이 역삼동이라
대치 동 필터(대치·도곡·개포동)에서 걸러진 것이었다. 선릉로·도곡로변 학원가는
법정동만 역삼동일 뿐 학부모 인식과 통학권 모두 대치다 → dong_list 에 역삼동 추가.
'PEAI' 처럼 영문 약칭으로 찾으면 못 찾는다 — 등록명은 '피아이어학원'.
**신고가 오면 registry 검색 전에 neis_raw 를 여러 표기로 검색할 것.**

한 단계 더 있다: **등록명과 브랜드가 아예 다른 학원.** '리딩타운'(SP대치
리딩타운)의 NEIS 등록명은 운영 법인인 **'그로튼에밀튼어학원'** 이다 —
글자가 하나도 안 겹쳐 어떤 매칭으로도 이어질 수 없고, 시드 별칭이 유일한
다리다. 이름 검색으로 안 나오면 **웹에서 실제 등록명(주소·전화로 교차
확인)부터** 찾을 것. 상세는 위키 `academies/323.md`.

시드로 추가해도 안 뽑히면 두 가지를 볼 것:
1. **strict 매칭 실패** — '해빛나인어학원1관학원' 은 정규화가 업종어를
   먼저 떼 rest 가 '1관' 이 되는데, 옛 검사식은 '관' 이 남아 단정을 막았다.
   지점 표기(_BRANCH)까지 지우고 판정한다.
2. **정원 경쟁** — 시드라도 정원이 작으면(그로튼에밀튼 151) 같은 구간의
   큰 시드에 영원히 밀린다. **한 번도 수집 안 된 시드는 최우선**으로
   뽑는다. 한 번 본 뒤에는 똑같이 경쟁한다.

**정원 편향은 한 층 아래에서 반복된다.** 구간 안에서도 정원순이라, 반포
초등 영어 후보 69곳 중 자리 19개를 대형 어학원이 채우고 학부모가 실제로
말하는 소규모(우승희 32명·잉글리쉬부띠끄 12명)는 32~60위로 영원히 안 보였다.
→ **수요 신호**(`demand.py`): 이미 수집한 글에 이름이 나오는 미수집 학원을
정원보다 앞세운다. 비교글·나열글은 근거로는 버리지만 **발견의 재료로는
가장 값지다** — 실측에서 해빛나인 176회·현재어학원 66회·리딩타운 51회가
미수집 상태였다. 점수에는 쓰지 않는다. 수집 대상 선정에만.

## 순위 학원이 사라졌다 — 기억이 잘려 나가는 파일에만 있었다

'렉스킴 학원이 유명한데 목록에 없다'는 신고. **선정 문제였고 원인이 둘**이다.

렉스김어학원은 대치 빅3 영어로 표본 111·총점 49.7 로 순위에 들어 있었고,
23회나 수집됐다(`collect_history`: tries 23 · dry 0 · last 115). 그런데
채점 대상에서 빠져 등록부에만 남아 있었다.

1. **기억이 잘려 나가는 파일에만 있었다.** `_previous_scores()` 는
   `academies.json` 을 읽어 '지난 회차에 순위였나' 를 판단하는데, 그 파일이
   바로 이 선정의 **결과물**이다. 한 회차 밀리면 다음 회차에는 '순위였던 적
   없는 곳' 이 되어 보호가 영영 사라진다 — **한 방향 문이다.**
   → 수집 이력에도 기억이 있다. `last`(마지막 회차에 실제로 모은 글 수)가
     표본 문턱을 넘었으면 자리를 지킨다. 이력은 잘리지 않는다.

2. **정렬로 앞세우는 것만으로는 모자랐다.** 구간·과목마다 자리가 열아홉 칸
   남짓이라 같은 우선순위 층이 넘치면 마지막 열쇠인 **정원**이 승부를 가른다.
   정원 60 인 렉스김은 대치 영어의 큰 학원들을 이길 수 없었다.
   → 근거가 선 곳은 **자리를 먼저 뗀다.** 순환은 아직 근거가 없는 자리에서.

**자리를 먼저 떼면 예산에서 그만큼 빼야 한다.** 구간별 할당을 고정으로 두면
지킨 자리가 예산에 더해져 대상이 불어난다(실측 400 → 442). 남은 예산으로
다시 나눈다.

실측: 렉스김 복귀(표본 120 · 총점 54.9), 순위 진입 78 → 82곳, 예산 400 유지.

**★ '순위에서 사라졌다' 는 신고를 받으면 점수부터 보지 말 것.** 먼저 물을 것:
그 학원이 이번 회차 **수집 대상이었나.** `registry.json` 에만 있으면 점수를
매긴 적이 없다는 뜻이고, 그건 근거가 나빠서가 아니라 우리가 안 봐서다.

**선정 규칙 셋이 서로를 밀어낸다 — 자리를 다투게 두지 말 것.**
1. 랭킹에 든 학원은 자리를 지킨다. 밀려나면 근거가 나빠져서가 아니라
   **우리가 안 봐서 화면에서 사라진다**(실측 83 → 68곳).
2. 한 번도 수집 안 된 시드는 반드시 한 번은 본다.
3. 둘을 같은 400칸에서 겨루게 하면 어느 쪽이 이겨도 사용자에게는
   '학원이 없다'로 보인다. → 신규 시드는 **예비석**(학군당 8칸)으로
   따로 태운다. 수집 예산은 API 호출 수일 뿐이다.

**표기가 하나 다르면 못 찾는다.** 'English Boutique' 의 등록명은
'잉글리쉬**부띠끄**어학원' 이다 — '부티크'로는 0건이다.

**`normalize_name` 은 업종어를 끝에서만, 복합형부터 뗀다.** 하나의
정규식으로 어디서든 지우면 '우승희영어학원' 이 '우승희영' 이 된다(실측
322곳이 …영어학원). 반대로 짧은 것부터 떼면 '씨앤씨어학원' 이 '씨앤씨어'
가 된다. 지점 표기(1관·별관)와 번갈아 벗겨야 '해빛나인어학원1관학원' 이
'해빛나인' 에 닿는다.

**--from-cache 는 coverage 를 기록하지 않는다.** 캐시 실행은 수집 시도가
아니다 — 기록하면 새 시드가 '수집했는데 0건'(dry)으로 오염돼 실제로는
검색해 본 적도 없는데 후순위로 밀린다.

## 랭킹 이력 — 없는 과거를 만들지 않는다

`app/assets/data/history.json` 에 매 수집마다 하루 한 점을 쌓는다.
캐시(.cache)가 아니라 **앱 번들 디렉터리**에 두는 이유: 야간 워크플로가
이 디렉터리를 커밋하므로 실행 사이에 살아남는다. .cache 는 비워질 수 있다.

- 같은 날 두 번 돌면 덮어쓴다. 하루 여러 점은 추이가 아니라 잡음이다.
- 순위는 **낮을수록 좋아서 차트 축을 뒤집는다.** 안 뒤집으면 매번 눈으로
  번역해야 한다.
- 점이 하나면 선을 그리지 않고 '집계를 시작한 날'이라고 말한다.
- 카드의 상승·보합 화살표(momentum)는 **언급량 추세**다. 순위 변동과 다른
  값이니 섞지 말 것.

## 교습비는 쓰지 않는다 — 비교가 성립하지 않는다

한때 컬럼명이 틀려(`THCC_CTNT` → 실제는 `PSNBY_THCC_CNTNT`) 전 학원이
비어 있었고, 고쳐서 38%를 살렸다가 **결국 통째로 뺐다.**

NEIS 는 금액만 주고 **교습시간을 주지 않는다.** 주2회 26만원과 주5회
26만원이 같은 값으로 나란히 선다. 시세도 지역·과목마다 달라 금액만으로는
읽히지 않는다. 비교가 성립하지 않는 숫자는 정보가 아니라 오해의 원인이다.

투명성 배점에서도 뺐다(교습비 공개 25점 → 나머지에 재분배).
`THCC_OTHBC_YN`(공개여부)은 애초에 9,324곳 중 9,224곳이 'Y' 라 신호가 없다.

**교습비를 다시 넣자는 이야기가 나오면 먼저 물을 것: 교습시간을 구했는가.**

**단, 이 규칙은 금액에만 걸린다.** 같은 필드(`PSNBY_THCC_CNTNT`)에 들어
있는 **과정명**('초등수학'·'중학내신'·'수능독해')은 학원이 스스로 밝힌
대상 학년과 과정이라 단계·구간 판정의 진짜 근거다. `neis.course_names()`
가 금액을 버리고 이름만 뽑는다 — 위 '단계별 학원 매칭' 참고.

## 가중치는 분산과 함께 봐야 한다 — '실효 기여'

평판·진입난이도를 각 0.35 로 올렸는데도 순위를 가른 것은 진입난이도뿐이었다.
가중치가 같아도 **분산이 다르면 영향이 다르다.**

    실효 기여 = 가중치 × 표준편차

    평판     0.35 × 2.49 = 0.87   ← 가장 낮았다
    진입난이도 0.35 × 9.36 = 3.27   ← 홀로 지배

원인: 평판이 감성 `[-1,1]` 을 `[0,100]` 에 선형 매핑했는데 실제 감성은
0.00~0.78 구간에만 살아서 점수가 53~65 로 눌려 있었다.
→ 화제성이 이미 쓰던 **코호트 z점수**로 통일(`50 + 15z`). 실효 기여 1.83 로 회복.

**기둥을 추가하거나 가중치를 바꾸면 반드시 실효 기여를 다시 재 볼 것.**
가중치가 뜻하는 바와 실제 영향이 어긋나면 산식을 공개하는 의미가 없다.

### 그리고 또 어긋났다 — 이번엔 반대 방향으로 (2026-08-30)

한 번 고쳐 놓고 손을 뗐더니 다시 벌어져 있었다.

    화제성   0.15 × 17.01 = 2.55   ← 0.15 짜리가
    평판     0.35 ×  4.03 = 1.41   ← 0.35 짜리를 눌렀다

원인이 셋이었고 **전부 '같은 것을 두 자로 재던' 문제**였다.

1. **축소의 분모가 사전표본과 단위가 달랐다.** `Σw` 를 그대로 썼는데 글
   한 건의 무게가 0.2~0.5 라, 표본 30건이면 `Σw ≈ 7` 이다. 사전표본 12 와
   견주면 축소 계수가 0.37 — 표본이 아무리 두꺼워도 사전값이 이긴다.
   → **Kish 유효표본수** `(Σw)²/Σw²`. 가중치가 고르면 표본 수와 같아서
     '사전표본 12건' 이라는 말이 실제로 12건을 뜻하게 된다.
2. **화제성의 자기 값과 코호트 분포가 다른 식이었다**(`log1p(건수)` 대
   최신성 가중합). → `volume_of()` 하나를 둘이 함께 쓴다.
3. **코호트의 μ 는 글 단위, σ 는 학원 단위였다.** z 의 중심과 폭이 서로
   다른 분포를 가리켰다. z 배수도 기둥마다 15/18 로 달랐다.
   → 둘 다 학원 단위 가중 평균으로, 배수는 `Z_SCALE` 하나로.

**★ 이제 매 실행 잰다.** `scoring.audit_contribution()` 이 실효 기여를
찍고, 가벼운 기둥이 무거운 기둥을 이기면 경고한다. 두 번 다 손으로 재서야
알았다 — 손으로 재야 아는 것은 언젠가 안 재게 된다.

## 등급반별 난이도는 만들지 않는다 — 표본이 없다

'그 학원 심화반 들어갈 수 있나'가 학부모의 실제 질문이라 반 이름 주변에서
난이도·대기 언급을 세어 봤다. 안 된다.

- 400곳 전체에서 반 이름과 난이도 표현이 함께 붙은 글이 **186건**뿐.
- 그중 상당수가 키워드 나열형 광고('파주 … 심화반 난이도 음악 미술').
- 결과가 **기초반이 최상위반보다 어렵게** 나왔다(최상위 37.4 vs 기초 34.2,
  정규반이 오히려 최고).
- 반 이름 주변 ±40자로 좁혀도 사다리가 24곳 성립 / 11곳 역전.

→ 반별 난이도는 큐레이션 테크트리의 **단계별 진입 기준**(exit_criteria)으로
  대신한다. 사람이 적은 것이라 근거가 분명하고 설명할 수 있다.

## 가중치를 화면에 상수로 박지 않는다

카드의 기둥 막대가 0.20/0.25 를 상수로 들고 있었다. 산식을 0.35 로 바꾼
뒤에도 목록은 옛 값을 그대로 보여줘, 상세의 35% 와 목록의 20% 가 어긋났다.
**가중치는 언제나 `meta.json` 에서 읽는다.**

표시 방식도 바꿨다. 네 칸을 같은 폭으로 두고 카드마다 '35%' 를 반복 적으면
같은 숫자가 카드 수만큼 반복돼 읽히지 않고, 정작 어느 기둥이 무거운지는
눈에 안 들어온다. → **칸의 폭이 곧 가중치**(`PillarWeightBars`, flex 비례).
숫자 표기는 상세 화면에서 한 번만(`showWeight: true`).
순서도 무거운 것부터 — 화면 순서가 곧 우선순위를 말한다.

## 진로 목적지 — 트리의 종점은 큐레이션이다

'테크트리는 목적성이 마지막 단계에 있다 — 유학·의대·조기졸업·서울대' 라는
요구. 단계가 '무엇을 배우는가' 라면 목적지는 '어디로 가는가' 다.
→ `techtree.yaml` 의 `destinations:` 7개. `config.destinations()`.

**★ 후기로 추론하지 않는다.** 목적지 신호를 실측하니 글 2.3%뿐이었다
(과목 12% · 방법 17% · 학급 7%와 견줘 가장 얇다). 학원 190곳 중 22곳만
분류된다. 하지만 '의대는 과탐II와 수학 최상위를 지난다' 는 후기로 알아낼
지식이 아니라 이미 아는 사실이다. **근거가 얇다는 것은 학원에 딱지를
못 붙인다는 뜻이지, 길을 못 그린다는 뜻이 아니다.**
- `requires` 는 **기존 단계 id 만** 참조한다. 새 단계를 만들지 않는다.
  없는 id 를 적으면 빌드가 경고한다 — 조용히 비면 '길이 없다' 는 거짓말이 된다.
- `linkable: false`(조기졸업 15건·예체능 28건)는 길만 그리고 학원을 안 잇는다.
- 경로는 사람이 적고 **얼마나 찼는지는 데이터가 말한다**(`stageCounts`).
  실측에서 의대의 `sci_hi_two`(과탐II)가 0곳, 영재고의 `math_mid_kmo` 0곳으로
  나왔다 — 큐레이션한 길의 어느 대목이 비었는지가 그대로 드러난다.

**관문(gates)이 이 구조의 알맹이다.** 초4 영재원·중2 KMO·고1 내신처럼
되돌리기 어려운 지점이 언제인지가 학부모의 실제 질문이다. 로드맵의 나이
축에 이정표보다 강하게 찍는다(목적지를 골랐을 때만 — 늘 띄우면 어느 길의
관문인지 알 수 없다).

**길 밖 단계는 지우지 않고 흐리게 둔다.** 없애면 '그 과목이 아예 없다' 로
읽히는데, 사실은 이 목적지가 그 단계를 요구하지 않을 뿐이다.

### ~~목적성은 조명이 아니라 축이다~~ — 축은 걷어냈다 (2026-08-31)

**목적성 축(목적지 × 과목 판, `destination_board.dart`)을 없앴다.** 아래는
왜 만들었는지의 기록으로 남긴다 — 다시 만들자는 이야기가 나올 때 같은
논거가 그대로 돌아오기 때문이다.

없앤 이유는 **판이 둘이면 맞춰야 할 것도 둘**이어서다. 목적지 선택·칸 수
세기·세 상태 표기를 두 곳에서 같게 유지해야 했고, 실제로 '숫자를 학군
기준으로 다시 센다' 같은 수정이 한쪽에만 들어간 적이 있다. 로드맵 안의
목적지 줄이 이미 길을 고르게 하고, 고르면 그 길이 밝아지고 관문이 나이
축에 뜬다 — 판을 하나 더 둘 만큼의 답을 더 주지는 못했다.

**목적지 데이터는 그대로다.** `destinations`, 단계 → 목적지 역방향
(`destinationsForStage`), 관문, 요약 모두 살아 있고 로드맵이 쓴다.
없앤 것은 **두 번째 화면**이지 개념이 아니다.

또 **'예체능 입시'(dest_art) 를 목적지에서 뺐다.** `requires` 가 통째로
비어 있어(실기 중심이라 교과 단계와 축이 다르다) 어느 화면에서도 길을
그리지 못했고, 빈 칸이 '데이터가 빠진 자리' 로 읽혔다.

<details><summary>당시의 논거</summary>

목적지를 로드맵의 **필터**로만 두면 한 방향에만 답한다. 학부모의 질문은
둘인데 방향이 반대다.

    과목 축(로드맵)   나이 × 과목    '초4 수학인데 이게 어디로 이어지나'
    목적성 축(신규)   목적지 × 과목  '의대를 보내려면 지금 무엇부터인가'

뒤쪽 질문은 조명으로 답할 수 없다 — **길을 고르기 전에는 어떤 길이 있는지
조차 안 보이기 때문이다.** 화면을 나누지 않고 축만 바꾼다(`TechTreeAxis`,
`destination_board.dart`).

- **선택은 한 곳에만 둔다**(`destinationProvider`). 화면마다 들고 있으면
  축을 바꿀 때 풀려서, 두 판이 같은 그래프라는 사실이 드러나지 않는다.
- **칸은 (목적지 × 과목)이다.** 목적지 하나에 학원을 한 줄로 세우지
  않는다 — 의대의 수학 학원과 과학 학원을 한 줄에 세우면 그 순위가
  무엇을 뜻하는지 설명할 수 없다(예체능을 학술과 안 세우는 것과 같다).
  학부모의 질문도 실은 이 단위다: '의대면 **수학은** 어디까지인가'.
- **세 상태를 다른 얼굴로 적는다.** 섞으면 판이 아무 말도 안 한다.

      지나지 않음   `·`        이 길은 그 과목을 요구하지 않는다
      잇지 않음     `—`        근거가 얇아 학원을 안 잇기로 한 길
      비었음        '비었음'    요구하는데 근거 있는 학원이 0곳

  `requires` 가 통째로 빈 길(예체능 입시)은 점 여섯 개로 두지 않는다.
  그건 '데이터가 빠졌다' 로 읽힌다 — 요약을 끌어와 **말로** 적는다.
- **숫자는 학군 기준으로 다시 센다**(`EduTreeData.legFor`). 파이프라인의
  `stageCounts`·`academyCount` 는 전국 합계라, 그대로 찍으면 학군을 바꿔도
  숫자가 안 움직이고 바로 아래 목록 길이와도 어긋난다.
- **엣지는 양쪽에서 읽는다.** 단계 시트에 '이 단계를 지나는 길'을 넣었다.
  목적지 → 단계만 있으면 로드맵에서 단계를 눌렀을 때 '이건 어디로
  이어지나' 에 답하지 못한다.
- **학원 → 목적지는 근거가 확실한 단계만**(`curated`·`hinted`). `inferred`
  (단서 없이 과목·학년으로 추정)까지 끌어오면 실명 사업자 페이지에 근거
  없이 '의대 학원' 딱지가 붙는다 — 해시로 단계를 흩뿌리던 것과 같은 잘못.
  실측 400곳 중 97곳에만 뜬다. 판의 칸 수는 `inferred` 도 세는데(열면
  카드마다 근거 등급이 적혀 있다), **딱지는 그 자리에서 단정이 된다.**

**여기서 두 번 틀렸다.**
1. 표의 줄을 `Row(crossAxisAlignment: stretch)` 로 짰다. 스크롤 안의
   Column 이라 세로가 열려 있어 '무한 높이' 로 눕는다. → `IntrinsicHeight`.
   **한 축만 정한 상자를 Row·Column·Wrap 에 넣을 때마다 나오는 그 함정을
   네 번째로 만났다.** 물을 것: 나머지 축을 누가 정하는가.
2. `TagMark` 가 페인트에서 터졌다 — **왼쪽만 굵은 획인 테두리에는
   `borderRadius` 를 줄 수 없다**(Flutter 가 균일한 테두리에만 허용).
   이미 있던 버그인데 릴리스에서는 assert 가 꺼져 조용히 지나갔다.
   목적지 칩을 고르면 늘 지나던 경로다. → 반경을 뺐다(2px, 이 크기에서
   보이지도 않고 각진 칸이 이 판면의 말투다).

</details>

## 로드맵의 자리는 계산한다 — 손으로 적은 lane 은 셋을 못 센다

'구간이 겹쳐 학원명이 안 보인다'는 신고. 원인이 **둘**이었고 증상만 같았다.

1. **같은 자리에 두 카드가 포개졌다.** 배치가 techtree.yaml 의 `lane`
   (사람이 적은 0/1)을 그대로 믿고 '겹치면 폭을 반으로 나눠 좌우에 세운다'
   였다. 셋 이상이 한 학년에 겹치면 자리가 모자라고(고3 수학은 내신·수능·
   최상위·N수 **넷**이다), 같은 lane 끼리 겹치면 위 카드가 아래 카드를
   통째로 덮는다. 실측 41단계에서 **16쌍**이 그랬다.
   → `RoadmapLanes` 가 겹침에서 자리를 계산한다(구간 그래프 색칠). yaml 의
     `lane` 은 **같은 학년에서 시작한 것들의 좌우 차례**로만 남는다.
   → **경로선도 같은 계산을 써야 한다.** 겹침 판정이 `_EdgePainter` 에 한
     벌 더 적혀 있었다 — 배치를 고치면 선이 옛 규칙에 남아 어긋난다.

2. **카드가 짧아 이름이 넘쳤다.** 학년 한 칸은 72px 인데 표제 두 줄과 학년
   표기를 빼면 20px 남짓이다. 거기에 학원 세 줄을 밀어 넣어 43px 넘쳤고,
   넘친 부분은 그려지지 않는다. → **들어가는 만큼만 싣는다.**

**좁은 칸에서는 점수를 뺀다.** 한 과목이 넷으로 갈리면 카드가 75px 인데,
이름과 점수를 한 줄에 우겨 넣으면 정작 찾으러 온 이름이 서너 글자로 잘린다.
근거의 종류(★ 랭킹 · ○ 표본부족 · … 구간)는 아이콘이 그대로 말하고, 점수는
눌러서 여는 시트에 온전히 있다.

## 순위를 못 매기는 구간에도 이름은 적는다

영어유치원(`eng_kindy`)은 `roadmap_only` 라 학원을 잇지 않는데, 그러면 칸이
비고 폴백이 **엉뚱한 파닉스 학원**을 채운다. 5~6세를 순위에 세울 근거가
없다는 판단은 그대로 두되, '그 구간에 무엇이 있나' 에는 답한다.
→ 단계에 `examples:`(사람이 적은 이름)를 두고 **점수도 등수도 붙이지 않는다.**

**등록부에 있다고 채점 대상인 것은 아니다.** 애플트리·게이트 어학원은
등록부에 학군마다 있지만 채점 대상이 아니다. 이름으로 찾을 때 조심할 것 —
채점 대상 중 '애플트리' 는 잠실의 **사고력수학교습소**로 전혀 다른 학원이다.

## 두 글자 낱말을 부분일치로 찾지 않는다 — '강의대상' 안의 '의대'

목적지 신호를 처음 재니 6%가 나왔고 그중 52%가 의대였다. 그런데 의대로
분류된 곳이 **폴리어학원·소마사고력수학·시매쓰·와이즈만** — 전부 초등
학원이었다. 본문을 열어 보니 카페 학원평가 정형글의 `4. 강의대상 & 과목`
에서 **'강의대상' 안의 '의대'** 가 걸린 것이었다. 정형글 전체가 의대 근거였다.

→ 홀로 설 수 있는 형태만 쓴다: `의대입시`·`의대반`·`의치한`·`수의대`.
  6% → 2.3%. **절반 이상이 허수였다.**

같은 함정을 하루에 세 번 만났다 — `뷰티`(뷰티풀마인드수학학원),
`논술`(논구술은 '논술' 을 부분문자열로 갖지 않는다), `의대`(강의대상).
**새 낱말을 어휘에 넣기 전에 등록부·본문 전수로 오탐을 확인할 것.**

## 통합 로드맵 — 유아 구간은 표시 전용이다

테크트리의 기본 얼굴은 통합 로드맵(`roadmap_view.dart`)이다. 세로가 나이
(-2 = 5세 ~ 12 = 고3), 가로가 과목. 카드의 위치·길이가 시작 나이와 기간이다.
과목·구간별 트리는 과목 칩으로 들어가는 상세다.

- **5~6세(영유 등)는 랭킹에 반영하지 않는다.** 그 나이대 학원을 순위에
  세울 근거 데이터가 없다. `roadmap_only: true` 단계는 구간 트리 분배에서
  빠지고(banded_techtree 가 건너뜀), 학원 매핑도 하지 않는다. 화면에는
  점선 테두리 + '로드맵 안내 · 순위 없음' 으로 나온다.
- 랭킹 구간(GRADE_BANDS)은 0(예비초)부터다. 학년 코드만 -2까지 내려간다.
- 이정표(roadmap_milestones)는 techtree.yaml 에 있다: 5세 영어 → 6세 수학 →
  예비초 국어 → 초4 재편 → 중등 전환.
- 로드맵 페이로드는 `config.roadmap_payload()` — 갈리지 않은 원본 트랙 기준.
  구간 트리(banded)와 별도로 techtree.json 의 `roadmap` 키로 나간다.

## 단계별 학원 매칭 — 근거의 등급을 나누고, 빈 곳은 화면에서 채운다

로드맵에서 단계를 눌러도 학원이 안 나오면 로드맵이 랭킹으로 이어지지 않는다.
'테크트리 목록이 비어 있거나 분류가 틀렸다'는 신고를 받고 전수로 재 봤다.

1. **자동 매핑**(`_auto_stages`) — 과목·학년 구간의 교집합에 드는 단계 중
   이름·교습과정·**과정명** 단서(`_STAGE_HINTS`)로 좁힌다. 단서가 없으면
   그 구간의 대표 단계(`_STAGE_DEFAULT`) 하나에만 붙인다.
2. **수집 순환**(`coverage.py`) — 수집했는데 언급 0건인 곳을 기록해
   다음 회차에서 뒤로 민다(2회 연속이면 후순위). 아직 3곳이 안 찬 단계의
   후보를 앞으로 당긴다. 매 야간 실행마다 조금씩 순환하며 **누적**된다.
3. **화면 폴백**(`academiesForStage(fillTo:)`) — 근거 있는 학원이 모자라면
   같은 과목·구간의 학원을 이어 붙이되 '이 단계 근거 없음 · 같은 구간'이라
   적는다. 랭킹의 '표본 부족' 폴백과 같은 방식이다.

**★ `curated_stages` 와 `stages` 를 구분할 것.** 수집 대상 선정이
`stages` 보유를 '유명 브랜드' 신호로 쓰는데, 자동 매핑으로 345곳이
stages 를 갖게 되자 신호가 죽어 랭킹이 83 → 65 로 떨어졌다.
선정은 `curated_stages`(시드 매칭)만 본다.

### 근거 없는 배정을 만들지 않는다 — 해시로 흩뿌리던 것을 없앴다

'선행'·'영재고 대비' 같은 세부 단계가 0곳으로 남는 것을 막으려고, 단서가
없는 학원을 **학원 id 해시**로 골라 보조 단계 하나에 더 붙이고 있었다.
전수로 재 보니 그 단계들의 배정이 사실상 전부 해시였다:

    math_mid_yeongjae (영재고·과학고 대비)  286곳  100% 해시
    math_mid_kmo      (심화 · KMO)         291곳  100% 해시
    kor_mid_preview   (고등 국어 선행)      605곳  100% 해시
    math_el_thinking  (사고력)            1,423곳   96% 해시

화면은 이것을 큐레이션 매핑과 **똑같이** 보여줬다. 실명 사업자를 근거 없이
'영재고 대비 학원'이라고 적은 셈이다. 없앴다.

대신 **배정마다 왜 붙었는지를 남긴다**(`stage_basis` → 앱 `stageBasis`):

    curated   사람이 적은 큐레이션 매핑        (딱지 없음 — 기본값)
    hinted    이름·교습과정·과정명에 단서       '교습과정에 단서'
    inferred  단서 없이 과목·구간 대표로 추정   '과목·학년으로 추정'
    band      단계 근거 없이 화면에서 이어 붙임  '이 단계 근거 없음 · 같은 구간'

`band` 는 데이터에 없다. **앱에서만 만든다** — 파이프라인이 지어낸 사실로
남으면 다음 사람이 그것을 근거로 읽는다.

### 대표 단계는 '가장 넓은 곳'이지 '가장 높은 곳'이 아니다

`_STAGE_DEFAULT[(math, elem_high)]` 가 **경시·심화**였다. 단서 없는 평범한
수학교습소 941곳이 전부 경시 학원이 됐다(잠실 경시 목록에 '청진컴퓨터교습소'가
있었다). 과학 초4~6 은 **영재원 대비**가 대표라 117곳이 그리로 갔다.

뿌리는 트리 쪽이었다. `math_el_basic`(연산·교과)이 초3에서, `sci_el_lab`
(실험·탐구)이 초4에서 끊겨 **초4~6 구간에 '기본' 단계가 아예 없었다.**
학교 진도와 연산은 초6까지 이어진다 → 둘 다 초6까지 넓히고 대표로 삼았다.
결과: 잠실 경시 11곳 → 1곳(이름에 '최상위'), 대치 경시 4곳 전부 단서 보유
(그중 하나는 '…수학올림피아드학원').

### 빈 `grade_bands` 는 '아무 구간도 아님'이 아니다

가장 크게 샌 곳. 앱의 필터(`_matchesFilters`)는 빈 구간을 **'특정 학년대에
한정되지 않음'** 으로 읽어 어느 필터에도 걸리게 두는데, `_auto_stages` 는
같은 값을 '해당 구간 없음'으로 읽어 단계를 **하나도** 안 붙였다.
등록부 6,092곳 중 **4,771곳(78%)** 이 여기 해당해 테크트리에서 통째로
사라져 있었다. 같은 값을 두 곳이 반대로 읽으면 반드시 한쪽이 틀린다.

### 교습비 필드에는 금액만 있는 게 아니다 — 과정명이 있다

`PSNBY_THCC_CNTNT` 를 통째로 안 읽고 있었다. **'교습비는 쓰지 않는다'는
금액 이야기다**(교습시간이 없어 비교가 성립하지 않는다). 같은 필드의
**과정명**은 다른 물건이다 — '초등수학'·'중학내신'·'수능독해' 는 학원이
스스로 밝힌 대상 학년과 과정이고, 학원명·교습과정목록에는 없다.
9,324건 중 1,836곳(20%)에 있다. → `neis.course_names()`, 금액은 버린다.
형식이 `과정명:금액, …` 인데 과정명 안에 쉼표가 들어가므로
('초등수학(주3회, 회60분):100000') **금액 자리에서 자르고 괄호를 지운다.**

### 결과

    화면에 아무것도 안 나오는 조합   16 → 0    (41단계 × 4학군 = 160)
    목록 3곳 미만                 59 → 0
    목록 중앙값                    3 → 8곳
    근거 없는(해시) 배정        3,000+ → 0

`--from-cache` 로 재현된다. 단계 목록이 다시 얇아 보이면 **먼저 물을 것:
근거가 없어서 비었나(정상), 아니면 근거를 못 읽고 있나.** `stageBasis` 를
보면 갈린다 — 전부 `band` 면 그 단계는 우리가 아직 아무것도 모르는 것이다.

## 타 사이트 후기 수집 — robots.txt 를 먼저 본다

스터디홀릭(studyholic.com)을 표본으로 쓰자는 요청이 있었으나 하지 않았다.
robots.txt 가 `User-agent: ClaudeBot / Disallow: /` 와
`Content-Signal: ai-train=no` 를 명시한다. 크롬 확장으로 열어 보는 것과
수집해 지표에 넣는 것은 다른 일이다. 도구를 바꿔 우회하지 않는다.

## 정정 요청은 어디로 가나 — 끝까지 따라가 볼 것

접수 → 저장 → 운영자 인지까지가 한 줄로 이어져야 창구다. 세 군데가
끊겨 있었고 전부 **조용한 실패**였다(화면에는 성공이라 떴다).

1. **산식 페이지 폼이 아무 데도 안 보냈다.** 입력을 받아 `_sent = true`
   만 하고 버렸다. 자체 안내문에 '백엔드 연결 전' 이라 적혀 있었는데
   그대로 배포돼 있었다. → `correctionServiceProvider` 로 연결.
2. **처리 대장이 접수를 못 봤다.** 기본 상태가 `received` 인데 조회는
   `status=eq.open` 만 봤다. 접수는 되는데 목록이 늘 비어 있었다.
   → `in.(received,open,reviewing)`.
3. **알림이 없었다.** 야간 작업이 로그에만 찍었다. → 미처리가 있으면
   종료코드 2 로 끝내 워크플로가 실패하고 깃허브 알림이 간다.

현재 경로:
  앱(로그인 불요) → Supabase `corrections` → 야간 작업이 미처리 감지
  → 깃허브 알림 → `corrections_report.py` 로 확인·처리

`--done <id앞자리>` 는 uuid 라 like·범위비교가 안 된다. 목록을 받아
파이썬에서 앞자리를 맞추고 1건일 때만 처리한다.

## 관련성 게이트가 못 잡는 것 — 동명이인

게이트는 '학원명이 본문에 있는가'만 본다. 사람 이름과 학원명이 같으면
못 거른다. 실측: '윤도영' 등장 글 146건 중 10건이 **뮤지컬 배우 윤도영**
관련이었고 그대로 평판에 반영됐다(대치 21위).

→ `/admin` 검수 화면 + `crawl_rules` 표.
  - 반려하면 사유를 묻고, 사유가 동명이인·무관·판매면 **규칙으로 굳힐지**
    이어서 묻는다. 여기서 규칙이 안 생기면 사람이 같은 글을 영원히 누른다.
  - 규칙은 `scope=academy|global`, `kind=exclude_keyword|require_keyword|
    exclude_domain|exclude_region`. 반드시 `reason` 을 받는다 — 이유 모르는
    규칙은 나중에 지우지도 못하고 남는다.
  - `exclude_region` 은 **제목만** 본다(다른 종류는 제목+본문). 본문의
    지역명은 대개 함께 언급된 남의 상호라 본문까지 보면 멀쩡한 근거가
    날아간다 — `branches.py` 에서 같은 함정에 두 번 빠졌다.
    사유가 '다른 지역 지점' 이면 제목에 실제로 나온 지역어만 초안으로
    채운다. 목록 전체를 채워 주면 사람이 읽지 않고 저장한다.
  - 파이프라인이 매 수집에서 규칙 → 판정 순으로 적용한다
    (`review_queue.apply_rules` → `apply_verdicts`).
  - **반려 말고 재분류.** 네 학원을 비교하는 글이 한 곳에만 붙는 일이
    있다. 반려밖에 없으면 멀쩡한 글이 통째로 버려지고, 어느 학원 글인지
    사람이 아는 정보도 함께 사라진다. `verdict=reclassified`(그 학원의
    근거가 아님) 와 `reassign_to`(대신 이 학원들의 근거) 는 **별개다** —
    넷 중 하나는 맞고 셋이 틀린 경우가 있다.
    파이프라인은 이미 수집된 원문을 복제해 지목된 학원에 붙인다
    (`review_queue.apply_reassignments`). 그 학원으로 수집된 적이 없어도
    붙는다 — 그게 재분류의 목적이다.
  - 검수 키는 `url_hash|academy_key` 다. 같은 글이 여러 학원에 걸릴 수
    있고, 한쪽에서 오검출이어도 다른 쪽에서는 정상 근거일 수 있다.
  - **판정은 학원별, 화면은 글별.** 키를 학원별로 쪼갠 탓에 '대치 빅3 영어
    학원 리뷰' 같은 글이 목록에 네 번 떴다(292건 중 61건이 중복 표시).
    화면에서는 원문 URL 로 묶어 한 장의 카드로 보여주고, 전체 확인/반려와
    학원별 개별 제외(칩의 ×)를 함께 둔다.
  - 큐 상한도 **글 수**로 센다. 판정 수로 세면 실제로 보는 글이 절반이 된다.
    같은 글에 걸린 학원은 함께 올린다 — 쪼개 올리면 다음 회차에 나머지가
    또 올라와 같은 글을 두 번 보게 된다.

윤도영에 `뮤지컬|공연장|커튼콜|배우…` 규칙을 넣어 122건 → 115건,
21위 → 29위로 정정됐다.

**RLS 주의.** 검수 표를 service_role 전용으로 두면 앱(anon)에서는 늘
빈 배열이 와서 '대기 없음'처럼 보인다. `admins` 표 + `is_admin()` 으로
로그인 운영자에게만 연다. 운영자 등록은 SQL 로만 한다.

## 예체능·기타 — 네 기둥을 다 적용하지 않는다

과목을 여섯으로 늘렸다(수학·영어·국어·과학 + 예체능·기타). NEIS 분야구분
`예능(대)`·`기예(대)` → 예체능, `기타(대)`·`직업기술`·`정보` → 기타.
독서실만 계속 제외한다. 등록부 4,089 → 5,650곳.

**예체능·기타는 평판 60% + 화제성 40%** 로만 채점한다(`NON_ACADEMIC_WEIGHTS`).
- 투명성을 빼는 이유: 교습과정 공시가 '미술' 한 줄인 곳이 많아, 상세도를
  재면 학원의 차이가 아니라 **공시 습관**을 재게 된다.
- 진입난이도를 빼는 이유: 레벨테스트·대기 개념이 없는 곳이 대부분이라
  신호가 안 잡힌다. **없는 것을 0점으로 치면 그게 곧 왜곡이다.**
- 그래서 `transparency`/`selectivity` 는 `null` 로 내보낸다. 0 이 아니다.
  앱도 `pillarOrNull()` 로 받아 없는 기둥은 아예 그리지 않는다.

**과목별 랭킹만 둔다.** 수학 학원과 미술 학원을 한 줄에 세우면 그 순위가
무엇을 뜻하는지 설명할 수 없다.

**선정에 예체능 몫(12%)을 따로 뗀다.** 학년 구간으로만 나누면 학년 단서가
약한 예체능이 매번 뒤로 밀린다 — 90곳을 뽑았는데 언급이 잡힌 곳이 2곳뿐이었다.

**`primary_subject()` 를 코호트와 산식이 공유해야 한다.** 다르면 예체능
학원이 수학 코호트에서 상대평가된다.

## 최신성 — 평판에만 걸려 있었다

'최근 글에 더 무게를' 요청을 받고 확인하니 반감기 180일 감쇠가 **평판에만**
적용돼 있었다. 화제성은 90일 창의 단순 개수, 진입난이도는 최신성을 아예
안 봤다 — 3년 전 '대기 걸렸다'가 지난달과 같은 무게였다.

→ 셋 다 적용. 화제성은 90일 창과 최신성 가중합 중 큰 값을 쓴다(경계가
  91일/89일에서 딱딱하게 갈리는 것을 완화).

**날짜를 아는 글은 9%뿐이다.** 카페 검색 API 가 날짜를 안 준다. 나머지는
0.6(6개월 된 글과 같은 취급)을 받아 최신성이 사실상 안 걸린다. `discovery`
가 발견 시점을 기록해 기준선 이후 새 글에는 발견일을 채우므로, 회차가
쌓일수록 나아진다.

### ★ 그 발견일이 추세를 통째로 거짓말하게 만들고 있었다 (2026-08-30)

카드의 상승·하락 화살표가 **학원의 화제성이 아니라 우리 크롤 일정**을
가리키고 있었다. 추세가 잡히는 44곳 중 **40곳이 '상승'** 이었다.

`first_seen` 의 '발견분' 27,533건이 **단 사흘**(8/22~24)에 몰려 있었고,
날짜를 아는 글의 81%가 그 사흘짜리 봉우리였다. 12개월 창의 맨 끝에 봉우리가
서 있으니 모두가 상승일 수밖에 없다.

뿌리는 **기준선 방어가 실행 단위로만 걸린 것**이다. 첫 실행은 기준선으로
막았지만, 수집 대상 400곳이 **회차마다 순환**하므로 어떤 학원을 처음
수집하면 그 학원의 몇 년치 글이 전부 '오늘 처음 본 것' 이 된다.
**새로 쓰인 것이 아니라 우리가 이제 본 것**인데도.

세 겹으로 막았다.
1. **학원 단위 기준선** — 이전 회차에 이미 보던 학원의 글에만 발견일을
   채운다. 처음 보는 학원이면 '어제 없던 URL' 이라는 말 자체가 성립하지
   않는다.
2. **소급 정정**(`discovery.mark_initial_sweeps`) — 어떤 학원의 도장 날짜
   중 **가장 이른 하루**가 그 학원의 첫 수집일이다. 그날 도장은 기준선으로
   되돌린다. 멱등이라 매 실행 돌려도 같다. (여러 학원에 걸친 글은 **모든**
   학원에서 첫 수집일일 때만 되돌린다 — 한 곳이라도 보고 있던 학원이
   있었다면 그쪽에는 진짜 새 글이다)
3. **추세 시계열에는 발견일을 아예 안 쓴다.** 최신성 가중치와 90일 창에는
   그대로 쓴다 — 거기서는 '최근에 처음 본 글은 대체로 최근 글' 이라는
   논리가 성립한다. **시계열의 x축을 우리가 만들면 안 된다.**

결과: 하락 26 / 상승 11, 나머지는 '보합'. 대부분이 보합인 것이 정상이다 —
실제 작성일을 아는 글이 9%뿐이라 **대부분 우리는 추세를 모른다.**

**연출·보정 값을 넣을 때 물을 것: 그 값의 x축을 누가 만드는가.**
우리가 만들면 그건 측정이 아니라 우리 일정의 그림자다.

## 대표명이 과하게 깎이면 그 이름이 곧 오염원이 된다

'책읽기' 학원의 근거가 전부 남의 이야기였다(리드101 레테, 대치도서관
독서토론, 루미북클럽…). 앞서 넣은 일상어 게이트로도 안 걸렸다 —
그 글들에 '레벨테스트'·'원장' 같은 학원 표지가 **다른 학원 이야기로서**
들어 있었기 때문이다.

뿌리는 통합 단계였다. `representative_name()` 이 '책읽기독서논술교습소' 와
'책읽기와글쓰기리딩엠역삼…' 의 공통 접두사 **'책읽기'** 를 대표명으로
삼았다. 3자라 조건은 통과하지만 이건 브랜드가 아니라 일상어다.
그 이름으로 검색하니 온 세상 독서 글이 딸려 왔다.

→ 접두사가 **원래 이름의 절반도 안 남으면 쓰지 않는다.** 업종어를 뗀
  알맹이 기준으로 견준다. CMS·소마 같은 정상 축약은 그대로 유지된다.

**증상이 '근거가 죄다 남의 글' 이면 표시명부터 볼 것.** 이름이 곧 검색어다.

## 표본 0 은 '적다' 가 아니라 '없다'

통합을 고치자 이 학원의 표본이 0 이 됐는데 총점 53.9 가 남아 있었다.
평판·진입난이도가 표본이 없으면 코호트 평균(50)으로 채워지기 때문이다.
랭킹에는 안 들어가지만(189곳이 같은 상태), 상세 화면에는 점수가 보인다.

→ 표본 0 이면 점수를 내지 않는다. 카드의 다이얼은 '—', 라벨은 '근거 없음',
  상세의 점수 구성 패널은 통째로 감추고 안내문을 낸다.
  '표본 부족' 과 같은 말로 적으면 50점이 평가 결과처럼 읽힌다.

## 학원명이 일상어일 때 — 동명이인과는 다른 문제다

**일곱 곳을 더 찾았다(2026-08-31).** '새로운학원이 오염됐다'는 신고를 받고
채점 대상 400곳의 **알맹이**(지역 접두어·업종어를 뗀 이름)를 전수로 훑어
표본 큰 순으로 근거 글을 열어 봤다. 같은 종류가 줄줄이 나왔다.

    테스트 201건 → 0   기파랑·에이프릴의 **레벨테스트** 후기가 전부 걸렸다
    피아노 196건 → 0   엄마표 독서·뮤지션 대담·남의 피아노 교습소
    새로운 166건 → 0   '**새로운** 영어학원을 찾고 계시나요?(러닝하이)'
    포인트 160건 → 0   '터닝**포인트**', 불당동 학원
    갈무리 148건 → 0   해양수산부 합격수기·게임 모음·아이스링크 강습
    로드맵 121건 → 0   'CMS 설명회', '초2 수학 **로드맵** 고민'
    음악    51건 → 0   실용음악·명일동·상일동 남의 음악학원

**전부 0 이 된 것은 과함이 아니라 정직이다.** 캐시 5만 글에서 '테스트'는
1,815건인데 '테스트학원'은 **1건**이다. 그 201건은 애초에 이 학원 이야기가
아니었다. 표본 0 은 '근거 없음'이지 '낮은 점수'가 아니므로, 지어낸 59.9 보다
낫다. 다만 진짜 근거도 함께 얇아지므로 **낱말을 넣기 전에 반드시 본문을
열어 볼 것** — 이 일곱은 하나씩 확인했다.

**전수로 훑는 방법이 이 문제의 열쇠다.** 신고는 한 곳으로 오지만 원인은
낱말이라, 같은 낱말을 쓰는 다른 학원도 같이 오염돼 있다.


'책읽기' 라는 학원이 실제로 있다. 이 말은 그냥 동사구이기도 해서
'아이 책읽기 습관' 같은 육아 글이 전부 그 학원 근거로 잡혔다
(382건 중 학원 맥락이 있는 것은 64%, 나머지는 일상 글).

**동명이인(윤도영)과 해법이 다르다.** 그쪽은 '뮤지컬' 같은 제외어로
막히지만, 일반 명사는 제외할 낱말을 특정할 수 없다. 대신 조건을 **더한다.**

1. `is_generic_name()` — 이름 전체가 일상어인 학원만 대상.
   **부분 일치로 보면 안 된다.** 처음에 '생각' 을 포함하는 이름으로
   잡았더니 '생각하는황소'·'깊은생각' 이 함께 걸렸다. 지역 접두어와
   업종어를 뗀 알맹이가 목록과 **정확히 일치**할 때만.
2. 학원 표지 요구 — '레테', '수강', '원장' 같은 말이 함께 있어야 근거로 친다.
3. **제목의 주인공 판정** — 제목에 다른 학원 이름이 있는데 이 학원 이름은
   제목에 없으면 버린다. 제보받은 예: '기파랑 레벨테스트를 치고' 라는 글이
   본문의 "책읽기를 좋아하고" 때문에 책읽기 학원 근거가 됐다.
   경쟁 학원명은 등록부까지 넣어 만든다 — 제목의 학원이 채점 대상이
   아닐 수 있다. 3자 미만은 우연히 겹치므로 뺀다.

결과: 표본 186 → 89, 대치 12위 → 26위. 영향받은 학원은 이 한 곳뿐이다.

### 이름이 아니라 **별칭**이 일상어일 때 — 시드 한 줄이 다섯 학원을 흔든다

'대치정상수학학원은 수학·영어를 결합했는데 이름이 수학학원이라 부적절하다.
진입난이도는 대부분 잘못된 정보다' 라는 신고. 둘 다 맞았고 원인이 셋이었다.

**1) 시드 별칭 `정상` 이 일상어였다**(`seed_academies.yaml`). 위 '책읽기' 는
이름 전체가 일상어라 `is_generic_name` 이 잡는데, 여기는 **이름은 멀쩡하고
별칭만** 일상어다. 그래서 아무 문턱도 없었다. 걸린 글들:

    '인테리어 금액 상담 어느 정도가 정상인지 …'  → 진입난이도 '정원 마감'
    '하이뮨 프로틴 … 정상적인 면역기능'         → 진입난이도 '대기·웨이팅'
    '호빵찜기기' · '변기막힘 출장 업체' · '진주과외'

이 말로만 걸린 글이 대치 76·서초 153·잠실 92·송파 122건. **지역을 안 밝힌
글은 형제 지점 전부에 붙으므로 별칭 하나가 다섯 학원의 점수를 흔든다.**

- **학원 표지를 요구하는 정도로는 못 막는다.** '금액 **상담**' 의 '상담' 이
  표지로 통과한다. 문턱을 높이는 대신 낱말을 후보에서 뺐다.
- **길이로 자르면 안 된다.** 2자 별칭 20곳을 재 보니 '소마'·'폴리'·'필즈'·
  '청담' 은 그 말로만 걸리는 글이 210·240·80·157건이고 대부분 진짜다.
  학부모가 실제로 그렇게 부른다. **기준은 길이가 아니라 일상어인지다**
  → `analyze.EVERYDAY_ALIASES`. 새 낱말을 넣기 전에 그 말로만 걸린 글을
  눈으로 볼 것 — '시대'(130건)·'강대'(206건)도 같은 냄새가 나지만 확인
  전에는 넣지 않았다. **지우는 쪽도 조용히 틀릴 수 있다.**

**2) 사건 낱말이 공백을 넘어 생겼다.** `flat` 은 공백·문장부호를 지운다.
'과학적인 8**대 기**능성' → '8대기능성' → **대기**. 건강식품 광고가
진입난이도 '대기·웨이팅' 근거가 된 경위다. → 낱말에 공백이 없으면 원문에서도
붙어 있어야 한다(`claims._contiguous`). '자리 없' 처럼 **공백을 품은 표기는
붙여 찾는 것이 목적**이라 이 검사를 건너뛴다.

**3) 대표명이 최단 이름이라 한 과목이 전체를 대표했다.**
'대치1관정상어학원'(영어·표본 337) + '대치정상수학학원'(수학·61) 이 묶여
화면에 **수학학원**으로 나갔다. → 쓸 만한 공통 접두사가 없으면 **가장 오래된
등록**(id 를 딴 그 등록)의 이름을 쓴다. id 는 이미 최고참에서 따는데 이름만
다른 기준으로 뽑으면 신원과 표시가 어긋난다. 관 표기가 걸리면 **번호만 떼고**
쓴다 — 관 없는 이름을 아무거나 집어오면 그게 바로 이 사고다.

**여기서 한 번 틀렸다.** 접두사 유효성 검사의 기준까지 최고참으로 바꿨더니
'메이플'·'뉴파인'·'브래니악' 이 '메이플리틀쇼팽음악학원' 으로 퇴화했다
(전수 35곳). 바꿀 것은 **폴백뿐**이었다 — 접두사 판정은 최단 이름 기준
그대로. 고치니 23곳/290곳으로 줄었고 남은 변경은 대체로 개선이다
(이강프리미엄**독서실** → 대치이강프리미엄**학원**, 모니카**원격**학원 →
목동모니카**영어**학원).

**신고를 받으면 물을 것: 이 학원을 가리키는 말 중 일상어가 있는가.**
이름만 보지 말고 **별칭까지** 볼 것.

## 업종어를 뗀 이름을 후보에 넣지 않으면 글이 엉뚱한 학원에 붙는다

'대치 빅3의 프로그램! 피아이 해빛나인 지엘피 잉글리쉬' 라는 지엘피 후기가
**피아이** 근거로 잡혔다. 원인은 두 겹이었다.

1. **후보 이름이 전체 이름 하나뿐이었다.** '지엘피아카데미학원' 은 있는데
   '지엘피' 가 없어, 본문의 '지엘피' 와 매칭되지 않았다. 사람들은 업종어를
   붙여 쓰지 않는다. → `name_candidates()` 가 업종어(학원·어학원·아카데미·
   에듀·스쿨·캠퍼스·센터·연구소)를 뗀 형태를 함께 넣는다. 3자 미만으로
   짧아지면 버린다 — 오매칭이 난다.
   이 하나로 정상 매칭이 늘어 랭킹 진입이 86 → 98곳이 됐다.

2. **여러 학원을 늘어놓은 글을 안 걸렀다.** 비교글·목록글·광고가 다 여기
   해당한다. 두 조건으로 본다:
   - 제목에 다른 학원이 2곳 이상인데 **내 이름은 제목에 없으면** 곁다리다.
     (내 이름이 제목에 있으면 남긴다 — '대치 깊은생각 레벨테스트 후기' 는
      다른 학원이 함께 나와도 이 학원 글이 맞다)
   - 본문에 다른 학원이 3곳 이상인데 **내 이름은 한 번뿐이면** 버린다.
     '에이프릴 vs 청담' 처럼 둘을 실제로 다루는 글은 내 이름도 여러 번 나온다.

**증상이 '엉뚱한 학원에 붙었다' 면 후보 이름부터 볼 것.** 매칭이 안 되는
학원이 있으면 그 글은 다른 학원 차지가 된다.

## 한 글은 한 학원에 대해 한 가지만 말한다 — 과목도 학급도

'랭킹 엔티티는 과목 하나를 가져야 한다. 한 글이 한 학원에 여러 과목으로
붙으면 안 된다. 학급 매칭에도 오류가 있다' 는 요구를 받고 재 봤다.

**과목이 집합이었다.** `subjects_near()` 가 set 을 돌려줘 같은 글이 수학
근거이자 과학 근거였다(실측 시대인재 379건 중 124건 중복, 와이즈만 225건
중 111건). 표본이 부풀면 그 학원이 실제보다 두껍게 논의된 것처럼 보이고,
**코호트 평균까지 함께 밀려** 남의 점수도 흔든다.
→ `subject_near_one()` — **이름에 가장 가까운** 과목어 하나만. 글이 그
  학원을 말하며 바로 옆에 둔 낱말이 그 글의 주제다.

**학급 신호는 아예 없었다.** 과목에는 `subjects_near` 가 있는데 학년
구간에는 대응물이 없었다. → `band_near_one()`, 같은 ±45자 창.
'초등' 처럼 구간을 못 정하는 말은 **어느 쪽으로도 넘기지 않는다** —
저·고학년은 사실상 다른 시장이라 반씩 나눠 가지면 둘 다 틀린다.

**★ 빈 grade_bands 가 '학급 오류' 의 정체였다.** 앱은 빈 값을 '어느 구간에도
한정되지 않음' 으로 읽어 **네 구간 모두**에 내보낸다(그게 맞다 — 종합·보습을
걸러내면 통째로 사라진다). 그런데 94곳이 비어 있었고, 그래서 수능 재종반
**시대인재가 '대치 예비초~초3 국어' 랭킹 1위**였다.
→ `_bands_from_mentions()` — 후기가 반복해 말한 구간만 확정(11곳).
  시대인재·플라즈마 → high, 와이즈만 → middle. 못 정하면 비운 채로 둔다.

### 학급별 **점수**는 아직 못 낸다 — 근거가 7%뿐이다

'학원을 학급 수만큼 엔티티로 가르자' 는 요구는 데이터가 아직 못 받친다.
실측(24,369건):

    과목 신호 있는 글   12%
    학급 신호 있는 글    7%
    둘 다 있는 글        6%

(학원 × 과목 × 학급) 엔티티는 채점 401곳만으로 **862개**다. 학급까지
밝힌 글이 1,470건뿐이라 엔티티당 평균 한 건이 안 된다 — 갈라 봐야 대부분
표본 0 이고, '조합마다 10곳' 요구와 정면으로 충돌한다.

그래서 **학급은 학원 단위로만 정하고 점수는 과목 단위로 낸다.** 학급별
점수를 내려면 수집이 더 쌓여야 한다. 회차가 지나면 다시 재 볼 것 —
`band_near_one` 이 이미 글마다 태그를 달아 두므로 재는 것은 언제든 된다.

## '보습·논술' 은 과목 선언이 아니다 — 827곳이 국어 학원이었다

'대치 예비초~초3 국어에 아이엘이가 있다. 아이엘이는 영어학원이다' 라는
신고. 맞았고, 뿌리가 깊었다.

`_infer_subjects` 는 이름에 과목 단서가 없으면 교습과정으로 물러선다.
그런데 NEIS 보습학원의 기본 등록값이 **'보습·논술'** 이고, 거기 '논술' 이
국어 힌트에 걸린다. 이름에 아무 단서가 없는 학원 **827곳이 국어**가 됐다 —
시대인재·강남대성·러셀·두각·대치파인만·CMS영재관·생각하는황소가 전부
국어 랭킹에 있었다.

**같은 경고가 이미 문서에 있었다.** `_subjects_from_name` 은 '교습과정
('보습·논술')은 종합학원 대부분이 그 값이라 신호가 아니라 잡음이다' 라고
적고 안 쓴다. 그 규칙이 **폴백 경로에는 적용되지 않았다.**
→ `_course_signal()` 이 '보습·논술'·'진학상담지도'·'보통교과'·'보습' 을
  먼저 지운다. 진짜 국어 학원은 이름에 단서가 있고 이름이 먼저 판정되므로
  잃는 것이 없다.

**★ 고치면 반대쪽이 무너진다 — 함께 봐야 한다.** 827곳이 `general` 이
되자 랭킹 진입이 84 → 65 로 떨어졌다. `general` 은 어느 과목 랭킹에도
안 들어가므로 **표본 342건짜리 시대인재가 화면에서 통째로 사라진다.**
'국어에 잘못 있는 것' 보다 '아무 데도 없는 것' 이 나은지는 자명하지 않다.

→ `_subjects_from_mentions()` — 남은 근거는 후기다. `subjects_near()` 가
  이미 글마다 **학원 이름 근처(±45자)** 의 과목어를 뽑아 두므로, 그것을
  학원 단위로 모아 **충분히 반복되는 과목만**(5건 이상 · 15% 이상) 확정한다.
  확정되면 general 을 뺀다 — general 은 '모른다' 는 뜻이고 이제 알기 때문.
  하나도 못 정하면 general 로 남는다. 억지 배정보다 안 나오는 편이 낫다.

  실측 15곳이 확정돼 랭킹 80 으로 회복. 시대인재 → 수학 255·국어 53·
  영어 41·과학 30(수학이 압도적), 플라즈마·와이즈만 → 수학·과학.
  아이엘이는 영어 244건만 남고 국어에서 사라졌다.

### 과목어가 **낱말 사이에 걸쳐** 생긴다 (2026-08-30)

이름을 부분 문자열로 훑으면 형태소 경계를 모른다. 등록부 5,749곳 전수:

    '독학**재수학원**' 의 '수학'   →  재수 학원 19곳이 수학 학원
    '권미나**국어학원**' 의 '어학'  →  국어 학원 65곳이 영어 학원
    '…**독서실**' 의 '독서'        →  독서실이 국어 학원

`normalize_name` 에서 이미 겪은 함정이다('우승희영어학원' → '우승희영').
거기서는 **끝에서만** 떼는 것으로 풀었고, 여기서는 **이음매에 공백을
넣어** 낱말이 서로를 넘지 못하게 한다(`build._subject_text`).

**★ 지우지 않고 가른다.** '국어학원' 을 통째로 지우면 '국어' 까지 사라져
진짜 국어 학원이 과목을 잃는다. 공백 하나면 '국어' 는 남고 '어학' 만 깨진다.

**★ 정확히 그 표기일 때만.** '시대인**재수학**스쿨' 은 '인재' + '수학' 이라
진짜 수학 학원이다 — `'재수학원'` 이 아니므로 안 걸린다. 여기를 넓게 잡으면
멀쩡한 수학 학원이 통째로 사라진다.

앞말이 뒷말을 한정하는 복합어는 따로 둔다: **'과학논술'은 과학**이지
국어가 아니다(`_NAME_COMPOUNDS`).

**교습과정으로 과목을 정할 때 물을 것: 그 값이 그 학원만의 것인가,
아니면 그 업종 전부가 갖는 값인가.**

## 예체능·기타에 학술 학원이 섞이던 것 — 원인이 둘

1. **이름 힌트가 공시 분야를 이겼다.** '수영수학교습소' 가 '수영' 때문에
   예체능이 됐다. 공시는 '입시.검정 및 보습' 인데. → 학술 과목 힌트를
   먼저 보고, 예체능 힌트는 **공시가 학술이 아닐 때만** 쓴다.

2. **'etc' 폴백이 두 가지를 섞었다.** 과목을 못 정하면 전부 `etc` 로
   뒀는데, 여기에 종합·보습·재종·컨설팅(학술)과 진짜 기타(직업기술 등)가
   함께 들어갔다. '대치수능선배2관학원' 이 기타 랭킹에 떴다.
   → 학술 분야인데 과목 미상이면 `general`(config.UNRANKED_SUBJECT).
     **어느 과목 랭킹에도 넣지 않고 순위도 매기지 않는다** — 어느 과목으로
     견줄지 정할 수 없으면 등수도 매길 수 없다. 등록부에는 남는다.

점검할 때 주의: 이름 정규식으로 훑으면 '만화학원' 이 '만화'→학술로 잡히는
식의 헛detection 이 난다. **공시 분야(realm)를 함께 봐야** 한다.
'아담리즈수학대치센터학원' 은 이름에 수학이 있어도 공시가 `기타(대)` 라
그대로 둔다 — 학원이 그렇게 등록했다.

## 배포가 반영 안 될 때 — 서비스워커 캐시

로고를 키웠는데 "그대로"라는 피드백을 받았다. 배포는 정상이었고, 브라우저에
등록된 **예전 서비스워커가 낡은 `main.dart.js` 를 붙잡고** 있었다.

- 빌드에서 끄는 것(`--pwa-strategy=none`)만으로는 부족하다. 이미 등록된 것은
  사용자 브라우저에 남아 계속 캐시를 물고 있다.
- `app/web/index.html` 이 매 로드마다 등록 해제 + 캐시 삭제를 수행한다.
- 순위가 매일 갱신되는 서비스라 오프라인 캐시보다 최신성이 중요하다.

### 봇이 민 커밋은 배포를 깨우지 못한다

야간 수집이 만든 데이터가 사이트에 없었다. 수집은 매일 성공했고 커밋도
남았는데, **그 커밋에 배포가 한 번도 안 붙었다**(실측 2026-08-29: `511befba`·
`b44721bd` 둘 다). 사이트는 사람이 무언가 밀 때까지 옛 번들을 내주고 있었다.

원인은 깃허브의 재귀 방지다. 워크플로가 기본 `GITHUB_TOKEN` 으로 민 푸시는
**다른 워크플로를 실행하지 않는다.** 로그에는 아무 오류도 없다 — 수집도 성공,
배포도 성공(다른 커밋의 것), 사이트만 옛것이다.

→ `deploy.yml` 에 `workflow_run`(수집 완료 연쇄)을 걸었다. 깃허브가 직접 거는
  연쇄라 토큰 제약을 받지 않는다. PAT 로 커밋하게 하는 길도 있지만 시크릿이
  하나 늘고 만료 관리가 붙는다.

**여기 함정이 둘 있다.**
1. `workflow_run` 은 **성공·실패를 가리지 않고** 발화한다. 실패한 수집을 그대로
   배포하면 배포가 안 되는 것보다 나쁘다 → job 에 `conclusion == 'success'`.
2. 연쇄로 들어오면 기본 체크아웃이 **수집을 시작한 시점의 커밋**을 집을 수 있다.
   그러면 새 데이터를 만들어 놓고 옛 번들을 배포한다 — 성공으로 보이는데
   내용만 안 바뀌는, 가장 알아채기 어려운 실패다 → `ref: main` 을 명시한다.

**배포 트리거를 손대면 물을 것: 그 푸시를 누가 미는가.** 사람이면 push 로 되고,
봇이면 안 된다.

---

## 외부 API에서 확인된 제약

- **학교알리미 OpenAPI 에는 졸업생 진로현황(진학 실적)이 없다.**
  제공목록은 학사/학생(학생수·교원), 재정/시설/설비, 보건/복지뿐이다.
  진학 실적은 웹 화면 조회 또는 에듀데이터서비스(EDSS) 신청·심사 대상이다.
  최근 3년치만 공시된다. → 진학 실적 기반 학교 랭킹은 이 키로 불가.
  요청 형식과 확인된 apiType 은 `pipeline/edutree/schoolinfo.py` 상단 참고.
  (sidoCode 는 교육청코드 B10 이 아니라 행정표준코드 11 이다. 여기서 한참 헤맸다.)

- **EDSS 대량자료는 학교가 익명화돼 있다.** 학교총개황·전출입및학업중단 등
  일괄 다운로드 파일에는 학교명이 없고 식별자가 `개방ID`(10자리)뿐이다.
  학교알리미 `SCHUL_CODE`(S로 시작)나 NEIS `SD_SCHUL_CODE` 와 맞지 않고
  시군구 컬럼도 없다. EDSS 파일끼리는 같은 ID 체계라 조인되지만(서울 1,036개교
  일치) 어느 학교인지는 알 수 없다.
  → 학교별 순위는 이 자료로 불가. 서울 전체 추이까지가 한계.
    학교명이 붙은 자료는 EDSS 맞춤형 신청·심사를 거쳐야 한다.

- **data.go.kr 키는 서비스별로 활용신청해야 한다.** 키 하나로 모든 API 가
  열리지 않는다. 신청하지 않은 API 는 `SERVICE_KEY_IS_NOT_REGISTERED_ERROR`.

- **네이버 지도 지오코딩 키와 지도 SDK 키는 별개다.** SDK 키(KEY_ID)는
  브라우저에 노출되고 도메인 허용목록으로 보호하며, 지오코딩은 시크릿까지
  필요해 서버에서만 쓴다.

## 손대기 전에 알아둘 것

- **표본 10건 미만도 순위에 넣되 '표본 부족'이라 적는다** (2026-08-26 변경).
  예전에는 통째로 뺐다. 그랬더니 96개 조합(4학군 × 6과목 × 4구간) 중 순위가
  10곳을 넘는 곳이 **하나뿐**이었다 — '반포 과학 초등'을 누르면 빈 화면이었다.
  등수를 감추는 대신 **근거의 두께를 함께** 보여주는 쪽으로 바꿨다.
  **단, 자리는 두께순이다** (2026-08-30 추가). 적는 것만으로는 부족했다 —
  실측 7개 조합에서 표본 1~2건이 표본 47~130건짜리를 제치고 **1위**였다.
  표본이 얇으면 평판·진입난이도가 중앙으로 축소되는데, 표본과 무관한
  투명성이 그 위에 얹혀 두꺼운 학원을 넘어선다. → `ranking()` 이 순위층
  안에서 `isRanked` 먼저, 그 아래 '표본 부족'. 등수를 감추는 게 아니라
  **자리가 근거의 두께를 말하게** 한다.
- **캐시 실행이 산출물을 비우지 않는지 볼 것.** `schools.fetch_all()` 이
  네트워크 **실패** 에는 캐시로 물러서면서 키 **없음** 에는 빈 목록을
  돌려줬다. 그래서 키 없는 환경에서 `--from-cache` 로 산식을 실험하면
  (문서가 권하는 사용법이다) schools.json 이 0KB 로 덮어써졌다.
  **새 데이터 소스를 붙일 때 물을 것: 키가 없을 때와 실패했을 때가
  같은 결과인가.** 둘 다 '지금 새로 못 가져온다' 는 같은 상황이다.
- **표본 0 은 여전히 순위를 매기지 않는다.** 점수가 코호트 평균(50)으로
  채워져 있어 등수를 붙이면 그건 평가가 아니라 기본값이다. 목록 아래에
  등수 없이 '근거 없음'으로 붙인다. **'아직 안 봤다'와 '보고서 낮았다'는
  다른 상태다** — 실명 사업자를 다루는 서비스다.
- **미수집 학원에 점수를 붙이지 않는다.** '평가했는데 낮음'과 '아직 안 봄'은
  다른 상태다. 등록부(`registry.json`)와 채점 대상(`academies.json`)을 나눈 이유.
- **관련성 게이트**: 학원명이 본문에 없으면 근거로 치지 않는다. 실측에서
  수집분의 68%가 그랬다. 가중치를 낮추는 정도로는 부족하다.
- **demo 모드는 통합하지 않는다.** 시드 주소가 동 단위라 학군이 한 덩어리가 된다.
- `--from-cache` 로 API 한도를 태우지 않고 산식을 실험할 수 있다.

## 지점 — 브랜드 이름으로 쓰인 후기를 어느 지점에 붙일 것인가

유명 학원은 전국에 지점이 있다. 수집은 학원 단위인데 후기는 브랜드 이름으로
쓰이므로, 그대로 두면 **분당 지점 후기가 대치 지점 점수**가 된다.
실측: 대치에이프릴어학원 언급 377건 중 60건이 타 지역 글.

`branches.py` 가 두 규칙으로 가른다.

1. **지역을 밝힌 글 → 그 권역 지점만.** 우리 4개 권역 밖(분당·평촌·송도…)을
   가리키면 어디에도 안 붙인다.
2. **지역을 안 밝힌 글 → 걸리는 지점 전부에 똑같이.** 한 곳에 몰아주면 추측이고,
   버리면 브랜드 근거가 사라진다. `branch_basis='brand'` 로 표시해 화면에
   '지점 불명 · 브랜드 공통' 이라 적는다 — **안 적으면 중복 신고가 들어온다.**

**★ 지역 판별은 제목이 우선이다.** 여기서 두 번 틀렸다.
  1. 제목+본문을 합쳐 봤더니 '세종 엄마표영어…' 글이 본문의 **다른 학원 상호**
     `대치M수학` 때문에 대치 근거가 됐다. 본문의 지역명은 대개 남의 상호다.
  2. 제목에 우리 권역 말이 함께 있으면 살렸더니 `[송도 논술 학원] 대치메이드학원
     송도점` 이 통과했다. **브랜드에 '대치'가 박힌 것**이었다.
     → 제목에 권역 밖 지역이 있으면 우리 권역 말이 함께 있어도 버린다.

지점 묶음 키는 `brand` → 없으면 `dedupe.brand_token`(지역·업종어 제거).
같은 권역 안 여러 관은 이미 dedupe 가 묶었으므로 **권역이 다른 것만** 잇는다.

`OTHER_REGION_WORDS` 에 지역어를 추가할 때는 **그 이름의 학원 브랜드가 있는지
먼저 볼 것.** '종로'를 넣으면 종로학원이 전부 타지역 글이 된다.

## 아파트 → 중·고 배정 후보는 세 곳까지, 성별을 함께 적는다

지도에서 단지를 누르면 중·고 학교군의 **가까운 순 3곳**을 거리와 함께 낸다
(`_zoneLine`, `_NearbyRow`). 11개교를 늘어놓기만 하면 화면이 학부모보다 덜
아는 셈이고, 한 곳으로 단정하면 거짓이다. 확률(%)로는 여전히 바꾸지 않는다.

**남고·여고를 안 밝히면 목록이 거짓말이 된다.** 대치 고교 학교군은 가까운
순 상위가 숙명여고·경기여고라, 아들만 있는 집에 '1순위 여고'라고 적어 놓는
꼴이었다. `zones.py` 가 `coed` 를 근거에 실어 `(남)`·`(여)` 로 표기한다.
공학은 안 적는다 — 대부분이 공학이라 매번 적으면 읽히지 않는다.

지도 정보창과 목록의 개수는 **같아야 한다.** 다르면 같은 값을 두 번 읽는다.

## 휠이 가리키는 값과 실제 선택이 어긋날 때 — 목록이 늦게 온다

"학군을 전체로 뒀는데 랭킹은 대치가 나온다"는 신고. 필터도 랭킹도 정상이었다.
**휠만 거짓말을 하고 있었다.**

학군 목록은 데이터가 도착해야 채워진다. 첫 빌드에서는 `['전체']` 하나뿐이라
기본값 `daechi` 를 못 찾고 `indexWhere` 가 -1 → 0번 칸('전체')에 섰다.
그 뒤 목록이 채워져도 `selected` 는 그대로여서, **값 변화만 보던
`didUpdateWidget` 이 아무것도 하지 않았다.** 화면은 '전체', 선택은 '대치'.

→ `didUpdateWidget` 은 **값 변화와 목록 변화를 함께** 본다. 목록이 도착한
  것이면 굴리지 않고 `jumpToItem` 으로 맞춘다(사용자가 굴린 게 아니다).
  못 찾았을 때 0 으로 뭉개지 않는다 — 그러면 '아직 안 왔다'와 '첫 칸이다'가
  구분되지 않는다.

**늦게 오는 목록을 쓰는 선택 위젯은 전부 이 함정이 있다.** 회귀 테스트를
붙여 뒀다(`widget_test.dart`, 고치기 전에는 실패한다).

## 이름이 안 겹쳐도 헷갈린다 — '브랜드' 옆의 '지역명+브랜드'

"씨매스학원과 반포씨매스학원은 같은 학원 아닌가"라는 물음. 아니다.
공식 지점 목록(cmath.co.kr)으로 확인한 결과 넷 다 다른 지점이었다.

    시매쓰학원        남부순환로315길 104 → 서초 방배점
    반포시매쓰학원      서초중앙로31길 14-13 → 서초 반포점
    도곡시매쓰보습학원    도곡로13길 5      → 강남 도곡점
    사고력수학시매쓰학원   도곡로 408       → 강남 대치점

표시명 중복 해소는 **이름이 똑같을 때만** 돌았다. '시매쓰학원'과
'반포시매쓰학원'은 다른 문자열이라 그냥 통과했는데, 학부모 눈에는 앞의
것이 **브랜드 본점**처럼 읽힌다. → 같은 학군에 `지역명+브랜드` 형제가
있는데 내 이름이 맨 브랜드면 동을 붙인다 → `시매쓰학원 (서초동)`.

**조건을 넓게 잡아 두 번 틀렸다.**
1. *브랜드 토큰만 같으면 붙인다* → 146곳이 갈렸다. 같은 동에 있는 여러
   관까지 잡혀 '깊은생각 (대치동)' 처럼 **구분은 안 되고 길기만 한** 이름이
   됐다. → 동이 형제와 **실제로 다를 때만.**
2. 그래도 26곳이 남았다. 토큰이 우연히 겹친 것들이었다 — '폴리어학원'과
   '폴리어수학학원'은 다른 학원이다. → 형제 이름이 `지역명+내이름` 또는
   `내이름+지역명` 일 때만. 6곳으로 줄었고 전부 진짜였다.

`core_name()` 은 업종어만 뗀다. `brand_token_light()` 는 지역어까지 떼므로
'반포시매쓰'도 '시매쓰'가 되어 이 판단에 쓸 수 없다.

## 로드맵은 휴대폰에서 한 과목씩 — 방향은 하나여야 한다

좁은 화면에서도 네 과목을 가로 스크롤로 밀게 두었더니, 세로로 긴 그림을
세로로 훑다가 과목을 바꾸려면 가로로도 밀어야 했다. **두 방향이 섞이면
한 손으로 못 쓴다.** → 700px 미만에서는 `PageView` 로 한 과목씩 보여주고
전환은 스와이프에 맡긴다(`RoadmapView.mobileMaxWidth`).

- 임계값은 실측이다: 축 52 + (최소 카드폭 150 + 사이 10) × 4 = 692.
- **판을 그리는 코드는 하나로 둔다.** `_board(subjects: [...])` 가 과목
  목록을 그대로 받으므로 전체(넷)와 한 과목이 같은 코드를 쓴다.
  경로선 좌표도 이 목록으로 계산되므로 따로 손댈 게 없다.
- 과목 칩이 곧 현재 위치 표시다. 점(dot) 표시를 따로 두면 같은 것을
  두 번 말한다.
- 세로 스크롤은 페이지마다 따로 둔다 — 넘겼는데 중간부터 시작하면
  어디를 보고 있는지 알 수 없다.

## 한 학원이 여러 건물을 쓸 때 — 관(館)으로 묶고, 지역어는 남긴다

"목동 씨앤씨가 중복"이라는 신고. 실제로 20개 주소에 27건이 등록돼 있었고
화면에는 `씨앤씨 (목동동로 55)`·`(오목로 286)`·`(목동서로 389)` … 다섯 줄로
떴다. 한 학원의 여러 관이지 다섯 학원이 아니다.

기존 통합은 **같은 주소**만 봤다. 관이 건물을 나눠 쓰면 안 묶인다.
→ 같은 학군에서 **관 표기만 지운 이름이 같고**, 그중 하나라도 관 표기
(`제N관`·`N관`·`신관`·`본관`·`별관`)를 달고 있으면 한 학원으로 본다.
419건 흡수(기존 302건 → +117), 씨앤씨는 23건이 한 곳이 됐다.

**세 번째 함정: 업종어를 짧은 것부터 떼면 '어학원'이 안 떨어진다.**
'아이엘이(별관)어학원' 의 hall_key 가 '아이엘이어' 로 남아 본관 '아이엘이'
와 안 이어졌다(신고로 발견). `_strip_suffix` 는 **일부러** 짧은 것부터 뗀다 —
'뉴클리어학원'을 '뉴클리'로 만들면 그건 옆 건물 '뉴클리온'의 **접두사**라
브랜드 매칭에서 남남이 묶이기 때문이다.
→ 둘로 갈랐다. `_strip_suffix`(접두 매칭용, 짧은 것부터) 와
  `_strip_suffix_strong`(hall_key 전용, 복합형부터). hall_key 는 같은 학군
  안에서 **완전일치**로 견주고 한쪽에 관 표기가 있어야 묶으므로 과하게
  떼도 안전하다 — '뉴클리'와 '뉴클리온'은 글자가 달라 안 걸린다.
  실측 11묶음이 새로 이어졌고 전부 같은 브랜드였다(아이엘이·SNT 4·8·11관·
  청어람수 1~6관·정상수학·CMS 목동). 결합 재검증 통과.

**여기서 두 번 틀렸다.**
1. *원본 이름이 같은 것끼리 묶자* → 하나도 안 걸렸다. '씨앤씨' 라는 등록은
   NEIS 에 **없다.** 그 이름은 '씨앤씨3관학원'·'씨앤씨5관학원' 의 공통
   접두사로 통합 단계에서 만들어진다. **대표명으로 견줘야 한다.**
2. *약(light) 토큰으로 묶자* → 209묶음이 나왔는데 **지점이 합쳐졌다.**
   `brand_token_light` 는 지역어를 떼므로 리드101 도곡·대치·개포가,
   책나무 대치·개포가 한 학원이 됐다. → 관 표기만 지우고 **지역어는 남긴다.**

관 표기가 있는 쪽이 하나도 없으면 묶지 않는다. '피아노교습소(권춘자)' 와
'피아노교습소(김옥선)' 은 이름이 같을 뿐 **원장이 다른 남남**이고,
그런 곳에는 관 표기가 없다.

통합체의 이름에 관 번호를 남기지 않는다. '길벗제2관보습학원' 은 5개 관을
합친 곳의 이름으로 읽히지 않는다 — 2관만 가리키는 말이다.
관 없는 이름이 있으면 그것을, 없으면 번호를 뗀다(`길벗보습학원`).

**하이씨앤씨는 따로 둔다.** 씨앤씨 공식 사이트(cncstudy.com)가 신관하이씨앤씨를
목동관으로 소개하지만, 하이씨앤씨는 자체 사이트(gohicnc.com)와 별도
등록번호로 고등부를 따로 운영한다. 학부모가 구분해 부르는 이름을 합치면
화면이 현실보다 덜 알려 준다.

**★ 네 번째 함정: 과목어를 업종어로 알고 뗐다 (2026-08-30).**
`_SUFFIX_STRONG` 에 `'수학학원'`·`'영어학원'`·`'국어학원'`·`'과학학원'`·
`'논술학원'` 이 들어 있었다. 그래서 `'대치정상수학학원'` 과
`'대치1관정상어학원'` 의 hall_key 가 **둘 다 `'대치정상'`** 이 되어,
**영어 학원과 수학 학원이 한 학원으로 묶였다**(정상 대치·목동 2묶음).

'과하게 떼도 안전하다' 던 근거(같은 학군 + 완전일치 + 한쪽에 관 표기)가
바로 여기서 무너진다 — 같은 브랜드의 영어관과 수학관은 그 조건을 전부
만족한다. **과목은 두 학원을 가르는 정보다. 지워 놓고 같아 보인다고
묶으면 남의 후기가 내 점수가 된다.** → 업종어만 뗀다. `'어학원'` 은 남긴다
(그건 과목이 아니라 업종이고, 길이순이라 `'학원'` 보다 먼저 걸려
'아이엘이(별관)어학원' 문제도 그대로 해결된다).

## 같은 학군 안 두 지점 — 권역 게이트로는 안 갈린다

시매쓰 반포점(서초중앙로31길)과 방배점(남부순환로315길)은 **둘 다 반포 학군**이다.
`branches.apply` 의 권역 판별은 둘을 똑같이 통과시키므로, 방배점 수집분 415건 중
**120건이 제목에 '반포' 가 든 반포점 글**이었다.

→ 같은 학군에 같은 브랜드의 형제가 있으면 **동네 말**로 가른다. 형제만 가진
  말이 제목에 있으면 내 글이 아니다(`locality_words`). 방배점에서 90건이
  빠졌고 반포점은 그대로다(비대칭이 정상 — 방배점 쪽에는 변별어가 없다).

**구(區) 이름을 변별어로 쓰면 안 된다.** 반포동·방배동이 모두 서초구라
'서초 시매쓰' 글을 방배점 것으로만 치면 반포점 근거가 통째로 날아간다
(실측 135건 → 62건). `BROAD_WORDS` 로 강남·서초·송파·양천을 뺀다.
NEIS 법정동에서 뽑을 때도 같은 필터를 태워야 한다 — 방배점의 등록 동이
'서초동' 이라 여기서 한 번 놓쳤다.

## 시매쓰 지점 대조 (2026-08-24, cmath.co.kr 공식 목록)

    강남 대치점   도곡로 408          사고력수학시매쓰학원
    강남 도곡점   도곡로13길 5        도곡시매쓰보습학원
    강남 압구정점  압구정로42길 13      — 신사동이라 수집 범위 밖(정상)
    서초 반포점   서초중앙로31길 14-13  반포시매쓰학원
    서초 방배점   남부순환로315길 104   시매쓰학원 (서초동)
    송파 잠실점   삼전로 91           시매쓰잠실직영보습학원
    양천 목동점   목동서로 299         시매쓰목동학원

전부 다른 지점이다. 통합 대상이 아니다. 같은 건물(도곡로 408)의
'판테라수학학원' 은 공식 목록에 시매쓰 판테라수학점으로 있으나 이름이
다른 별도 브랜드라 따로 둔다.

목록 페이지는 EUC-KR 이고 페이징 파라미터는 `GotoPage` 다(`page` 는 안 먹는다).

## 분류 위키 — 지식은 상수가 아니라 페이지에 쌓는다

`pipeline/wiki/` 는 karpathy LLM wiki 패턴(원자료/위키/스키마 3계층)을 따르는
분류 지식 베이스다. 규약은 `pipeline/wiki/SCHEMA.md` — **위키를 손대기 전에
그 문서부터 읽을 것.**

- **읽기**: 학원 페이지 frontmatter(`aliases`·`generic`·`exclude_title`·
  `exclude_any`·`locality`)가 게이트에 힌트로 들어간다(`wiki.py`).
  대표 용도: NEIS 법정동에 변별어가 없는 지점의 동네 말
  (방배점 `locality: [방배]`), 영문 약칭(`PEAI`).
- **쓰기**: 매 실행이 표본·순위·판정·규칙·재분류를 페이지의
  `<!-- auto:* -->` 블록에 되적는다. 마커 밖 산문은 절대 건드리지 않는다.
  페이지는 랭킹 진입 또는 사람 손길(판정·규칙·재분류)이 닿을 때 생긴다 —
  등록부 5천 곳의 빈 껍데기를 만들지 않는다.
- **모순 점검**: 별칭이 남의 학원 이름이거나 제외어가 자기 이름을 치면
  그 힌트를 죽이고 경고를 찍는다. 잘못된 힌트의 조용한 적용이 최악이다.
- **검수와의 관계**: 위키는 관리자 검수를 대체하지 않는다. 최종 제외
  권한은 판정·`crawl_rules` 에 있고, 위키는 그것을 기록하고 힌트를 보탤
  뿐이다. Supabase 규칙과 같은 낱말을 위키에 중복으로 넣지 말 것.
- 야간 워크플로가 `pipeline/wiki` 를 함께 커밋한다 — 실행마다 자란다.

신고 처리 절차가 바뀌었다: 원인을 찾으면 코드 상수에 박기 전에
**해당 학원 페이지에 근거와 함께 적고**, 엔진이 처리할 수 있는 것은
frontmatter 힌트로, 패턴이면 `concepts/` 로 올린다.

## 글도 노드다 — 후기 한 건이 파일 한 장, 그 파일이 근거의 스위치

후기는 41MB JSON 안에만 있었다. 점수에는 쓰이는데 **가리킬 수가 없었다.**
'저 글은 가짜다'·'3년 전 이야기다' 라는 말을 받아도 붙일 자리가 없었고,
무효화 수단은 Supabase 판정 하나뿐이었다. → `posts.py`, `wiki/posts/<url_hash>.md`.
관련성 게이트를 통과한 글에만 만든다(수집분 2.9만 중 9.4천). 규약은 SCHEMA.md.

**무효화가 이 구조의 존재 이유다.** frontmatter 에 `verdict: rejected` 와
사유를 적고 다시 돌리면 그 글의 영향이 전부 사라진다 — 그 학원의 표본·평판만이
아니라 **코호트 평균과 남의 등수까지**. 파이프라인이 매 실행 전부를 원자료에서
다시 짓기 때문이다. 부분 삭제라면 '어디까지 지워야 하나'를 사람이 판단해야
하지만 재빌드에는 판단할 것이 없다.
실측(`pipeline/tests/test_posts.py`): A1 후기 12건 중 6건 반려 → A1 은 표본
부족으로 랭킹에서 빠지고, **후기가 하나도 안 줄어든 A2 의 총점이 30.5 → 32.3**.

네 가지를 구분해서 적는다. 섞으면 되돌릴 수 없다.
- `verdict: rejected` + `reject_reason` — 가짜·무관·광고. **사유 없으면 엔진이
  무시한다.** 이유 모르는 제외는 나중에 지우지도 못하고 남는다.
- `stale_after` — **낡음은 가짜가 아니다.** 3년 전 '대기 걸렸다'는 거짓이
  아니라 지난 사실이다.
- `reassign_to` — 진짜 주인에게 준다. 반려만 가능하면 네 학원 비교글이
  통째로 버려지고 '어느 학원 글인지'라는 사람이 아는 정보도 함께 사라진다.
- `verdict: confirmed` — 사람이 봤고 맞다.

**엔진은 frontmatter 를 절대 덮어쓰지 않고, auto 블록을 되읽지 않는다.**
엔진이 쓴 것을 엔진이 근거로 삼으면 한 번의 실수가 영원히 자기를 증명한다.

**요약은 점수에 쓰지 않는다.** `auto:digest` 는 이미 계산된 값(감성·신뢰도·
과목)을 옮겨 적을 뿐이라 새로 추정하는 값이 없다. `auto:summary` 는 LLM
문장인데 **키가 있을 때만** 채워지고(`summarize.py`, url_hash 로 캐시해 다시
만들지 않는다) 게이트도 산식도 이 값을 보지 않는다. LLM 문장이 산식에
흘러들면 그때부터 산식을 설명할 수 없다.

**그래서 제공자를 갈아끼울 수 있다.** 점수에 안 쓰이므로 어느 모델이 썼는지가
산식의 설명을 바꾸지 않고, 값싼 쪽으로 옮겨 다녀도 된다. 실제로 그렇게 됐다 —
Anthropic 키는 잔액이 없었고 Gemini 키가 있어 그쪽으로 붙였다.
`OPENEDU_SUMMARY_PROVIDER`(anthropic|gemini), 비우면 있는 키로 자동.
제공자를 늘릴 때 건드릴 곳은 `_call_*` 함수 하나뿐이다.

**소용없는 재시도를 하지 않는다.** 잔액 부족·권한 없음·없는 모델은 9천 번을
더 불러도 같은 답이 온다. `_fatal()` 이 한 번 보고 즉시 멈추고 사유만 남긴다
(실측: 9,362번 → 1번, 0.9초). 안 그러면 로그가 같은 오류로 도배되고 정작
무엇이 문제인지가 안 보인다.

**Gemini 는 사고 예산을 꺼야 한다.** `thinkingConfig.thinkingBudget=0` 을
안 주면 `maxOutputTokens` 를 사고가 먼저 먹어 본문이 빈 채로 끝난다.
껐는데도 간헐적으로 자리가 모자라 **문장이 중간에 끊긴다**(실측 1,091건 중
8건: '12월 1일 개', '수학 9'). 두 겹으로 막는다.
1. `finishReason != STOP` 이면 실패로 보고 **저장하지 않는다.**
2. 그래도 이미 캐시에 들어간 것은 다시 안 만들어지므로, 읽을 때
   `_looks_truncated()` 로 한 번 더 보고 다시 만든다.

**'한 번만 만든다'와 '잘린 것은 다시 만든다'는 함께 가야 한다.** 캐시는
비용을 아끼려고 두는 것인데, 잘린 조각을 영원히 붙들고 있으면 아낀 것이
아니라 잡음을 굳힌 것이다.

**엣지는 양쪽에서 적는다.** 글 쪽 `auto:edges`, 학원 쪽 `auto:posts`.
어느 쪽에서 읽어도 그래프를 따라갈 수 있어야 한다.

## 공식 홈페이지 — URL 은 추측하지 않는다

NEIS 는 홈페이지 주소를 주지 않는다. 학원 페이지 frontmatter 의 `homepage:`
에 **사람이 적은 것만** 읽는다(`official.py`). 이름으로 검색해 맞히려 들면
남의 사이트를 그 학원 것이라고 적게 된다 — '지엘피'·'피아이'처럼 이름이
겹치는 곳이 이미 많다.

- **robots.txt 를 먼저 본다.** 거부하면 읽지 않고, 가져오다 실패해도 읽지
  않는다(없는 것과 실패는 다르다). 스터디홀릭을 수집하지 않기로 한 것과 같은
  기준이다. `Content-Signal: ai-train=no` 도 거부로 친다.
- **본문을 저장하지 않는다.** 남의 저작물이다. 남기는 것은 '무엇이 몇 개
  있고 지난 확인 대비 얼마나 달라졌나'와 출처 URL·확인 날짜뿐이다.
- 2주에 한 번, 한 실행에 40곳까지. 학원 홈페이지는 매일 바뀌지 않는다.

## 로컬 파이썬은 3.12 — 요약만 3.10+ 를 요구한다

macOS 기본 파이썬은 3.9(`/usr/bin/python3`)이고 `anthropic` 1.x 는 3.10+ 다.
`.venv`(brew python@3.12)로 돌린다 — `.venv/bin/python pipeline/run.py`.
3.9 로 돌려도 파이프라인은 온전하고 **요약만** 꺼진다.

**선택 의존성 한 줄이 전체 설치를 막을 수 있다.** `anthropic>=1.0` 을 마커
없이 적었더니 3.9 에서 `pip install -r requirements.txt` 가 통째로 실패했다.
→ `; python_version >= "3.10"`.

**빈 환경변수와 미설정은 다르다.** GitHub Actions 는 정의 안 된 `vars` 를
빈 문자열로 넘긴다. `int(os.getenv(X, "200"))` 는 기본값이 안 먹고
`int("")` → ValueError 다. 환경변수로 숫자를 받으면 반드시 빈 값을 막을 것.

## 음의 0 — 파이썬 버전이 바뀌면 안 바뀐 페이지가 갈린다

`f"{-0.0:+.2f}"` 는 `-0.00` 이다. 중립인데 부정으로 읽히고, 더 나쁜 것은
같은 계산이 3.9 에서 `0.0`, 3.12 에서 `-0.0` 이 되어 **내용이 안 바뀐
페이지가 갈린다.** 9,361장 중 2장이 실제로 그랬다(교차 검증에서 잡혔다).

→ `posts._signed()` 가 **표시 자릿수로 반올림한 뒤** 0 이면 부호를 떼고
  `+0.00` 으로 적는다. `-0.001` 도 화면에 `0.00` 이라 적을 값이므로
  마이너스를 붙이지 않는다.

**파이썬 버전을 올리면 산출물을 바이트 단위로 견줄 것.** 실측에서
academies.json·techtree.json 은 3.9 와 3.12 가 완전히 같았다(채점은
결정적이다). 갈린 것은 표시 문자열 두 곳뿐이었다.

## Supabase 미러 — 지울 것과 절대 못 지울 것

야간 수집이 몇 밤 연속 마지막 단계에서 실패했다. 원인이 **세 겹**이었고,
하나를 고칠 때마다 다음 것이 드러났다.

1. **`05_grade_bands.sql` 이 적용된 적이 없다.** 코드는 `grade_band` 를
   쓰는데 DB 는 `school_level` 이었다 → `Could not find the 'grade_band'
   column`. 수집·채점이 끝난 **뒤에** 터져서 JSON 과 사이트는 멀쩡했고,
   그래서 오래 안 띄었다.
2. **05 가 뷰와 RPC 를 놓쳤다.** 컬럼을 바꿔도 `v_ranking` 의 **출력 이름**
   과 `ranking_for` 의 **파라미터 이름**은 그대로 남는다(만들 때 고정된다).
   `create or replace` 로는 못 바꾸니 지우고 다시 만들어야 하고, 뷰를 다시
   만들면 **권한이 사라지므로** 원래 권한도 되돌려야 한다.
   → `12_grade_bands_finish.sql` (멱등).
3. **미러가 미러가 아니었다.** `on_conflict=id` 로만 upsert 해서 옛 행이
   남았다. 구간을 가르며 트랙 id 가 바뀌자(`math_elementary` →
   `math_elem_low`·`math_elem_high`) 옛 4행이 `unique(subject, grade_band)`
   를 점유해 새 트랙이 못 들어갔다. → `prune()`.

**★ 그런데 `academies` 는 절대 prune 하면 안 된다.** 수집 대상 400곳은
회차마다 순환하므로 이번에 안 뽑힌 학원이 늘 있다. 지우면
`user_reviews`·`bookmarks`·`corrections`·`score_history` 가 전부
`on delete cascade` 로 함께 사라진다 — 사용자가 쓴 후기와 정정 요청,
되돌릴 수 없는 순위 이력이다. 미러에 학원이 쌓이는 것은 오류가 아니라
'한 번이라도 평가한 곳'이라는 뜻이다.
**지우기를 넣기 전에 물을 것: 여기에 사람이 쓴 것이 매달려 있는가.**

**한 배치에 같은 키를 두 번 넣지 않는다.** 경계에 걸친 단계(사고력 초1~초4)
는 두 구간 트랙에 **모두** 든다 — 한쪽에서 빼면 길이 끊기기 때문이다.
그대로 보내면 Postgres 가 거부한다("ON CONFLICT DO UPDATE command cannot
affect row a second time"). `stages` 는 track_id 를 하나만 갖는 표라 그
사실을 담을 수 없으므로 첫 트랙에 매단다(실측 49행 → 41행). 앱은 이 표가
아니라 정적 JSON 을 읽으므로 화면에는 영향이 없다.

## 네트워크 한 번 끊겼다고 실행 전체를 잃지 않는다

`schooldistrict.status()` 가 자격 증명 **확인** 단계에서 `requests` 예외를
안 잡아, 연결이 잠깐 안 되면 야간 작업이 시작도 못 하고 죽었다. 수집 쪽
`fetch_all()` 도 마찬가지였다 — 거기까지 오면 NEIS·언급 분석·채점이 이미 다
끝난 상태인데 그걸 통째로 버렸다. 둘 다 캐시가 있고 학교 배정은 하루 이틀
묵어도 바뀌지 않는다. → 잡고 한 줄 찍고 계속 간다.

**새 네트워크 호출을 추가할 때 물을 것: 이게 실패하면 이번 실행이 통째로
날아가는가.** 부산물이면 잡아야 한다.

**같은 함정에 두 번 빠졌다.** `schooldistrict` 를 고치고 `schools` 를 안
고쳤더니 다음 실행에서 똑같이 죽었다. 두 곳 다 캐시가 있고, 학교는 하루
이틀 사이에 새로 생기지 않는다. → 둘 다 `requests.RequestException` 을
잡아 캐시로 진행한다.

**대량 동시 호출은 자기 발등을 찍는다.** 요약 백필(동시 16 × 9,362건)이
TIME_WAIT 소켓을 4,000개 넘게 남겨 바로 뒤 실행이 포트를 못 잡았다
(`Errno 49 Can't assign requested address`). 백필 직후에 파이프라인을
돌리려면 소켓이 빠질 때까지 잠깐 기다릴 것.

## 지식 그래프 — 무결성은 만들 때가 아니라 매 실행 증명한다

노드=학원(id=NEIS 등록번호, 통합 시 **가장 오래된 등록**의 것),
엣지=후기→학원(`academy_key` + `branch_basis`: direct/region/brand/reclassified).
`graph.py` 가 매 실행 감사한다: id 유일 · 등록 서로소(이중 흡수 0) ·
**결합 재검증**(묶음의 각 이름이 접두사/관/동일 중 하나로 이어져야 함) ·
고아 엣지 0. 통합 규칙이 바뀌면 이미 묶인 그룹이 여기서 재심사된다 —
아니면 옛 오결합이 화석이 된다.

**대표 id 는 이름 길이순이 아니라 개설일순.** 이름순은 더 짧은 관 하나가
새로 등록될 때마다 학원 신원이 뒤집힌다. id 에는 후기·판정·정정·위키가
전부 매달려 있다. 그래도 바뀌는 경우를 위해:
- 흡수된 등록 id 를 `registrationIds` 로 내보내고, 앱 `academyById` 가
  옛 id 도 해석한다(옛 링크 생존).
- 판정(`url|옛id`)·규칙·재분류 대상은 빌드 초입에 id_alias 로 현재 id 에
  이어 붙인다 — **운영자의 판단이 고아가 되면 같은 글을 두 번 심사한다.**
- 위키 페이지 이관은 sanity 경고를 보고 사람/LLM 이 한다.

'브랜드 공통' 딱지(`branch_basis='brand'`)는 형제 지점이 실제로 있을 때만.
지점 하나뿐인 학원에 붙이면 화면 칩이 거짓말이 된다.

## 검수는 글더미가 아니라 질문으로 — 답 하나가 규칙 하나

글마다 읽는 검수는 관리자 시간이 병목이고 판정 한 건의 가치가 낮다.
방향을 뒤집었다: 파이프라인이 판단이 필요한 지점을 찾아 **질문**을
만들고(`questions.py` · `review_questions` 표), 관리자는 `/admin` 의
'질문' 탭에서 답만 한다. 실행당 최대 12건 — 많으면 다시 노동이 된다.

질문 다섯 종류(전부 결정적 생성, LLM 없음):
- `confirm_post` 일상어 이름·브랜드 공유 글 중 고신뢰만 골라 분류 확인
- `exclude_word` 반려 글들에 반복되고 확인 글에는 없는 낱말 → 답이 곧 규칙
- `locality` 형제 지점인데 변별어 없음 → 답이 곧 위키 frontmatter
- `author` 동일 작성자 8건+ → 홍보 계정 확인 → exclude_author 규칙
- `compare` 감성이 갈리는 고신뢰 글 2건 → 답은 그 두 글의 신뢰도
  보정(±20%)만. **산식 전체를 흔드는 데 쓰지 않는다.**

원칙:
- **답의 적용 경로는 기존 검수 체계 그대로다.** confirm→mention_reviews,
  규칙→crawl_rules, 동네 말→위키. 질문은 입구일 뿐이다.
- `apply_answers()` 는 load_rules/load_verdicts **보다 먼저** 돈다.
  순서가 바뀌면 답이 한 회차 늦게 반영된다.
- fingerprint unique — 같은 질문을 두 번 묻지 않는다. 건너뛰기도 답이다.
- 글별 검수·재분류 화면은 보조 수단으로 남아 있다(두 번째 탭).

## 랭킹은 과목별로 — 같은 학원이라도 근거가 다르다

'과학에 시대인재수학스쿨이 있다'는 신고. 원인이 둘이었다.

**1) 시드 과목을 그대로 물려줬다.** `시대인재수학스쿨학원` 이 시드
'시대인재'(국영수과)로 strict 매칭돼 4과목을 받았다 — 이름이 수학
전문이라고 말하는데도. → **이름에 과목이 박혀 있으면 그쪽이 더 구체적인
사실이다**(`_subjects_from_name`). 교습과정('보습·논술')은 안 쓴다 —
종합학원 대부분이 그 값이라 신호가 아니라 잡음이다. 과목을 좁히면
단계(stages)도 그 과목 것만 남긴다.

**2) 점수가 과목별로 갈리지 않았다 — 이쪽이 근본이다.**
한 학원에 점수 하나뿐이라 수학 후기 496건짜리가 그 점수 그대로 과학
랭킹 3위였다. → `scoring.compute_all()` 이 **(학원 × 과목)** 마다 점수를
낸다. 규칙:
- 과목이 하나인 학원: 모든 글이 그 과목 근거.
- 여러 과목인 학원: **글이 그 과목을 명시해야** 근거로 친다.
  과목 불명 글은 **대표 과목에만** 넣는다.
- 코호트(상대평가)·표본 부족 판정도 전부 과목별.
- 앱은 `academy.scoreFor(subject)` 로 읽는다. 카드·정렬·표본 부족
  목록까지 전부 이 값을 본다.

**★ 과목어는 학원 이름 근처(±45자)에서만 센다.** 글 전체에서 찾으면
'대치동 부동산 가격형성요인' 이 과학 근거가 된다(실측: 시대인재 과학
태그 31건 중 대부분이 그런 글이었다). `branches.py` 가 '본문의 지역명은
대개 남의 상호' 라는 것을 배운 것과 같은 함정 — **낱말이 있다는 것과
그 학원 이야기라는 것은 다르다.**

부수 수정: `cohort_for` 가 `subjects[0]`, `build_cohorts` 가
`primary_subject()` 를 써서 어긋나 있었다(CLAUDE.md 가 경고하던 바로 그것).
이제 둘 다 과목 인자를 받는다.

결과: 대치 과학 1위가 시대인재(수학 근거) → **김태호과학학원**(과학 전문,
표본 11건)으로 바로잡혔다.

## 디자인 언어 —「實錄 / The Annals」

화면의 성격을 '앱'이 아니라 **문서**로 잡았다. 이 서비스는 실명 사업자를
다루는 기록물이지 추천 광고가 아니고, 조형이 그 말을 먼저 해야 한다.
토큰은 `core/theme.dart`, 조형은 `widgets/annals.dart`(+`annals_controls.dart`).

**축마다 결정을 적어 둔다. 하나만 바꾸면 나머지가 다 어긋난다.**

    바탕    순백이 아니라 한지 #F4F0E6. 흰 화면은 값이 0 인 배경이지만
            종이는 그 자체로 '기록물'이라는 맥락을 깐다.
    구분    그림자가 아니라 계선(界線). 실록 판면의 세로 괘선에서 왔다.
            카드를 띄우지 않고 줄로 가른다 — 떠 있는 것은 광고고,
            줄로 갈린 것은 기록이다. `PaperGround` 가 앱 전체에 깐다.
    모서리  알약(pill)을 쓰지 않는다. 2~4px. 종이에 그은 칸은 둥글지 않다.
            `AppRadius.pill` 은 남겨 뒀지만 정말 원이어야 하는 곳만.
    강조    주묵(朱墨) #C4392A **하나**. 넓게 칠하지 않고 찍는다 —
            1위·현재 위치·검증 도장에만.
    회색    푸른 기 없는 따뜻한 재색. 종이 위에서 파란 회색은 뜬다.
    숫자    장부 숫자(`ledgerFigures`, tabular). 등수·점수·표본은 세로로
            견주는 값이라 자릿수가 흔들리면 못 읽는다.
    배치    가운데 정렬을 쓰지 않는다. `IndexRail` 이 왼쪽에 색인 여백을
            고정으로 비우고 집계일·권차를 넣는다. 좁으면 **위로 올린다**.
    모션    '떠오르며 흐려짐'을 쓰지 않는다. 획을 긋고(`StrokeIn`)
            도장을 찍는다(`SealPress`).

**도장은 단정할 수 있는 것에만 찍는다.** 이 서비스에는 확인된 사실과
추정이 섞여 있다. 추정에 도장을 찍으면 그 순간부터 도장이 아무 뜻도
갖지 않는다. 데모 배너에 도장이 없는 이유가 그것이다.

**새 화면을 만들 때 Card·Chip 을 새로 그리기 전에 `annals.dart` 를 볼 것.**
Material 기본 컨트롤(`SegmentedButton`·`FilterChip`·`ChoiceChip`)은 전부
양 끝이 알약이라 각진 판면에서 **그것만 둥글어** 혼자 튄다.
`RuledSegments`·`RuledToggle`·`NameChip`·`TagMark` 로 대신한다.

### 여기서 네 번 틀렸다

1. **등장 애니메이션이 레이아웃 폭을 바꿨다.** `StrokeIn` 을 처음엔
   `Align(widthFactor:)` 로 열었다. `Align` 은 자기 폭을 자식 폭 × 비율로
   잡으므로 **애니메이션 도중의 폭이 곧 실제 배치 폭**이 된다. 랭킹 목록
   캡처에서 카드마다 오른쪽 끝이 다른 자리에서 잘려 있었다.
   → `CustomClipper<Rect>` 로 **그리기만** 자른다. 배치는 늘 온전하다.
   회귀 테스트가 `widget_test.dart` 에 있다(고치기 전에는 실패한다).

2. **`ColoredBox` 는 자식이 없으면 세로로 0 이 된다.** 산식의 '자(尺)'
   막대를 `Row` 안에 `Expanded(child: ColoredBox(...))` 로 뒀더니 교차축
   제약이 느슨해 높이 0 으로 그려져 **막대가 통째로 안 보였다.**
   → `crossAxisAlignment: CrossAxisAlignment.stretch`. 색만 칠하는 상자를
   Row/Column 에 넣을 때마다 물을 것: 교차축 크기를 누가 정하는가.

3. **작은 도장의 한자는 얼룩이 된다.** 21px 짜리 `Seal('實')` 은 안쪽
   테두리가 글자 자리를 먹어 '붉은 네모 안의 점'이 됐다.
   → 24px 미만이면 안쪽 테두리를 빼고 글자를 키운다. 글자도 **획이 적은
   것**으로 고른다 — 검증 도장은 `實`(14획)이 아니라 `正`(5획)이다.
   같은 이유로 활자 삼각형(▲)도 8px 에서는 점으로 보인다. 10.5px 이 최소.

4. **화면에 마크다운 별표가 그대로 찍혔다.** Flutter `Text` 는 마크다운을
   해석하지 않는다. `'**폭이 곧 가중치**입니다'` 가 별표째 나왔다.
   문서에 쓰던 강조 표기를 문자열에 그대로 옮기지 말 것.

### 곁다리로 고친 것

- **표본 0 인데 상세 머리에 점수가 떠 있었다.** 같은 화면 아래에는
  '점수를 매기지 않았습니다' 라고 적혀 있었다(실측: 근거 0건인데 40).
  카드는 이미 '—'로 처리하고 있었고 상세만 빠져 있었다.
  → `academy_page` 도 카드와 같은 규칙으로 맞췄다.
- **표시 이름 없는 과목 키가 꼬리표로 나갔다.** `general` 은 '어느 과목인지
  모른다'는 뜻인데 카드에 영문 `general` 이 그대로 붙었다.
  → 이름이 없으면 찍지 않는다. 모른다는 것은 붙일 꼬리표가 없다는 뜻이다.

### 헤드리스 캡처로 확인할 때

`--window-size` 를 작게 줘도 **크롬이 `innerWidth` 를 500 아래로 내려주지
않는다**(실측: 390 을 줘도 500). 캔버스만 390 으로 잘려 나오므로 화면이
넘친 것처럼 보인다. 좁은 폭의 배치는 캡처가 아니라 위젯 테스트로 볼 것.
`--force-prefers-reduced-motion` 을 함께 주면 등장 동작이 꺼져 캡처가
안정된다(그 경로도 함께 검증된다).

## 스크롤 연출 — 가져온 것과 두고 온 것

토스에서 가져온 것은 **스크롤 서사**다. 화면을 내리는 동안 값이 채워지고
숫자가 세어지며 다음 이야기가 열린다. 두고 온 것은 **스크롤 고정(pin)**이다.

토스는 판을 화면에 붙여 두고 스크롤을 애니메이션 재생기로 쓴다. 마케팅
페이지에서는 그 값을 치를 만하지만, 여기는 학부모가 **찾으러** 오는 자료
화면이다. 스크롤이 내 뜻대로 안 움직이면 연출이 아니라 고장으로 읽힌다.
→ 고정하지 않고 **보이는 동안의 진행도**만 값으로 쓴다. 부품은
`widgets/scroll_stage.dart`.

    Reveal          화면에 들어올 때 한 번 획을 긋는다 (되감지 않는다)
    Scrub           지나가는 동안의 진행도 0~1 (되감지 않는다)
    Tally           장부 숫자를 세어 올린다
    ReadingProgress / ReadingBand   머리띠가 읽은 만큼 찬다

**산식의 자(尺)가 이 층의 전부다.** 네 기둥을 누적 비율로 이어 **한 획**으로
긋는다 — 획이 지나가는 시간이 곧 그 기둥의 폭이고, 폭이 곧 가중치다.
다 그으면 정확히 100%. 연출과 값이 같은 것을 말한다.

**숫자는 막대와 같이 세지 않는다.** 같이 세면 중간에 '평판 22%' 같은 값이
화면에 뜬다. 잠깐이라도 틀린 가중치를 보여 주는 것은 근거 없는 숫자를
내지 않는다는 규칙과 정면으로 어긋난다. 참값을 흐림에서 꺼낸다.

**목록에는 걸지 않는다.** 한때 `AcademyCard` 가 스스로 획을 그었는데, 랭킹은
100곳이 넘고 목록은 스크롤할 때마다 항목을 만들었다 지운다 — 되올릴 때마다
같은 카드가 다시 그어져 부산스러웠다. 줄마다 움직이는 목록은 훑어 읽기도
어렵다. → 카드는 조형만 갖고, 열지 말지는 **화면이 정한다**(발췌 세 장만).

### 여기서 네 번 틀렸다

1. **스크롤 콜백에서 조상을 찾으면 죽는다.** `_onScroll` 안에서
   `Scrollable.of(context)` 를 불렀더니 위젯이 떨어져 나간 뒤 콜백이 한 번 더
   돌면서 `Looking up a deactivated widget's ancestor is unsafe` 로 터졌다.
   **화면을 옮길 때마다** 나는 종류다. → 의존성은 `didChangeDependencies`
   에서 한 번만 집어 두고(`_scrollable`), 그 뒤로는 저장해 둔 것만 쓴다.
   위젯 시험이 잡았다(브라우저에서 보기 전에).

2. **`late final` 컨트롤러는 dispose 에서 처음 만들어진다.** 필드
   초기화식(`late final _c = AnimationController(vsync: this …)`)은 게을러서,
   모션을 끈 사용자처럼 한 번도 안 읽히는 경로에서는 `dispose()` 가 그것을
   **처음 읽으며 만든다** — 이미 죽은 State 위에 티커를 다는 셈이다.
   → `initState` 에서 만든다.

3. **한 축만 정한 상자는 나머지 축에서 접힌다.** `SizedBox(height: 3)` 를
   Column 에 넣었더니 교차축 제약이 느슨해 **폭 0** 으로 눕고 머리띠가 통째로
   사라졌다. 앞서 `ColoredBox` 가 Row 안에서 **높이 0** 이 됐던 것과 같은
   함정을 축만 바꿔 반복한 것이다. 색만 칠하는 상자를 Row/Column 에 넣을
   때마다 물을 것: **나머지 축 크기를 누가 정하는가.**

4. **진행도를 얻자고 브랜드 표시를 잃었다.** 머리띠의 '안 읽은 쪽'을 주묵
   0.22 로 뒀더니 첫 화면에서 거의 안 보였다. 늘 있던 주묵 줄이 사라진 셈이라
   진행도보다 잃은 것이 컸다. → 0.45. '조금 옅은 주묵 줄'로 읽히고, 읽은 쪽이
   그 위에 온전한 주묵으로 덮인다.

### ★ 웹에서는 '동작 줄이기'가 통째로 막혀 있었다

브라우저는 `prefers-reduced-motion: reduce` 로 분명히 말하는데(실측: 크롬이
`matchMedia(...).matches === true`) Flutter 의 `MediaQuery.disableAnimations`
는 **false 였다.** 위젯마다 그 값을 보고 연출을 끄도록 짜 뒀는데, 정작 이
서비스가 도는 곳에서만 그 길이 안 열려 있었다.

→ `core/reduced_motion.dart` (조건부 import) 가 웹에서 미디어 쿼리를 직접
읽고, `main.dart` 의 `builder` 가 **한 곳에서** `disableAnimations` 를 얹는다.
아래의 모든 연출이 그대로 따른다 — 위젯마다 다시 물어보게 두면 언젠가 한
곳을 빠뜨린다.

**연출을 넣을 때 물을 것: 그걸 끈 사람에게 내용이 남는가.** 진행도는 1,
숫자는 최종값이어야 한다. 동작이 정보를 쥐고 있으면 못 보는 사람에게는
내용까지 사라진다.

### 헤드리스 캡처는 애니메이션 중간을 찍는다

`--virtual-time-budget` 을 크게 줘도 Flutter 의 프레임과 어긋나 **중간 상태**가
찍힌다(실측: 장부 숫자가 5,441 에서 멈춘 채, 표제가 획 중간에서 잘린 채).
잘린 화면을 보고 배치 결함으로 오해하기 쉽다. 완성 상태를 보려면
`--force-prefers-reduced-motion` 을 함께 줄 것 — 위 수정 이후로는 이 플래그가
Flutter 까지 닿는다.

## 과목 줄 — 순서 하나, 모양 하나

같은 조작이 화면마다 다른 얼굴이었다. 전수로 세어 보니 **모양 넷, 순서 둘**:

    랭킹          밑줄 탭              수학부터
    로드맵(모바일)  꽉 채운 알약(999px)   영어부터 (코드에 박혀 있었다)
    후기 쓰기      ChoiceChip           수학부터, 기타 뺌
    후기 거르기    FilterChip           수학부터, 있는 것만

모양이 넷이면 같은 조작인 줄 모르고, 순서가 둘이면 손이 기억을 못 한다.

**순서는 `models.subjectOrder` 하나뿐이다.** 근거는 채점 대상의 과목 수다
(실측 2026-08-28, 400곳): 수학 111 · 영어 88 · 국어 74 · 과학 48 ‖
예체능 59 · 기타 39. 수는 야간 수집마다 흔들리니 날짜를 적어 둔다 —
**중요한 것은 값이 아니라 차례이고**, 회차가 바뀌어도 차례는 유지됐다.
학술 넷이 앞, 예체능·기타가 뒤. 뒤 둘은 **산식이 아예 다르다**(평판 60% +
화제성 40%) — 한 줄에 두되 사이를 계선으로 끊는 이유가 그것이다.
아무 데서 온 목록이든 `orderedSubjects()` 로 세워 쓴다. 꼬리표·검색 결과·
지도 정보창도 전부 이 순서다 — 같은 학원인데 화면마다 꼬리표 차례가
달라지면 그것도 불일치다.

**로드맵의 옛 순서(영어부터)에는 뜻이 있었다** — 시작 나이 순이다. 그래도
바꿨다. 로드맵은 **세로축이 이미 나이**라, 가로까지 나이를 말하면 같은 것을
두 번 말하면서 다른 화면과는 어긋난다.

**모양은 `widgets/subject_bar.dart` 하나가 갖는다.**

    색 사각   과목 색은 꼬리표·로드맵·트리가 이미 쓰는 코드다. 고르지 않은
             칸에도 늘 보여 줘 **색과 과목의 짝을 이 줄이 가르치게** 했다.
    밑줄     선택 표시는 **주묵**이다. 과목 색으로 그으면 예뻐 보이지만
             주묵은 '지금 여기'를 뜻하는 하나뿐인 신호라 내비·필터와 뜻이
             어긋난다. 정체성은 사각이, 위치는 밑줄이.
    계선     학술과 예체능·기타 사이. 장식이 아니라 산식이 갈리는 자리다.

**바꾸지 않은 것:** 상세의 '과목별 점수'는 **표본 수 내림차순**을 유지한다.
그건 고르는 줄이 아니라 값으로 세운 표이고, 순서 자체가 '이 학원은 어느
과목 근거가 가장 두꺼운가'를 말한다. 랭킹 목록을 학원 이름순으로 세우지
않는 것과 같은 이유다.

### 색 코드는 두 판에서 똑같이 읽혀야 코드다

안료 색은 한지 위에 앉으라고 낮춰 잡은 값이라 먹빛 바탕에서 가라앉는다.
7px 짜리 색 사각에 쪽빛 `#2E4E8A` 를 `#101218` 위에 두니 사각인지도 안
보였다. → `AppColors.subjectOn(key, dark)`. **과목 색이 필요한 곳은 전부
이걸 쓴다** — `subjects[key]` 를 직접 읽으면 어두운 판에서만 조용히
가라앉아서, 밝은 화면만 보고 다니면 영영 못 찾는다.

### 여기서 두 번 틀렸다

1. **`'art'` 로 적어 두고 `'arts'` 로 찾고 있었다.** 표시 이름 쪽은 `arts`,
   색 쪽은 `art` — 키가 어긋나면 예외가 나지 않고 **조용히 회색**이 된다.
   예체능만 색 코드에서 빠진 채였다. → 두 표의 키가 맞는지 보는 시험을
   붙였다(`모든 과목이 이름과 색을 갖는다`).

2. **폭을 안 정한 상자는 최대까지 펼친다.** 학술/비학술 계선을
   `SizedBox(height: 34, child: Center(…))` 로 뒀더니, `Center` 가 폭 제약이
   느슨할 때 최대까지 펼쳐 **계선 하나가 1132px 덩어리**가 됐다. Wrap 의 한
   줄을 통째로 먹어 예체능·기타가 다음 줄로 밀렸고, 정작 선은 안 보였다.
   → 폭을 못 박는다.

   **같은 함정을 축만 바꿔 세 번째 만났다.** 앞서 `ColoredBox` 가 Row 에서
   높이 0 으로 접혔고, `SizedBox(height:3)` 이 Column 에서 폭 0 으로 접혔다.
   이번엔 반대로 펼쳐졌다. 한 축만 정한 상자를 Row·Column·Wrap 에 넣을
   때마다 물을 것: **나머지 축을 누가 정하는가.**

## 후기는 요약하지 않고 분해한다 — 주장(claim)이 근거의 최소 단위

글 한 건이 스칼라 몇 개(`sentiment`·`spam_score`·`selectivity.hard`)로
요약돼 집계로 넘어갔다. 요약된 뒤에는 **어느 문장이 그 숫자를 만들었는지
되짚을 수 없다.** 그래서 두 가지를 못 했다.

1. 학부모가 실제로 묻는 것(숙제량·시험 횟수·수업 시간)을 근거와 함께
   남기지 못했다. 감성 점수 안에 녹아 사라졌다.
2. **한 글의 일부만 취소하지 못했다.** `posts.py` 의 무효화는 글 전체를
   끈다. '이 진입난이도 근거는 틀렸지만 평판 근거는 맞다'를 담을 자리가
   없었다.

→ `claims.py`. 주장 하나가 행 하나이고 반드시 원문 인용문을 들고 다닌다.
  인용문을 못 보여주는 주장은 만들지 않는다.

**id 는 위치도 인용문도 아닌 알맹이(글·학원·종류·값)로 짓는다.** 스니펫은
회차마다 다른 길이로 잘려 오므로 위치로 지으면 판정이 고아가 된다. 인용문
으로 지어도 마찬가지다 — 앞에 한 마디가 더 붙으면 문장 경계가 달라진다
(여기서 한 번 틀렸다). **인용문은 그 주장의 증거이지 신원이 아니다.**

**Supabase 에 claims 표를 만들지 않는다.** 주장은 매 실행 원자료에서 다시
지어진다 — 미러링해 봐야 다음 실행이 덮어쓸 값이고, 표가 하나 늘면 그만큼
어긋날 자리가 생긴다. 영속해야 하는 것은 **사람의 판정**뿐이다
(`claim_verdicts`). `claim_id` 에 외래키도 걸지 않는다 — 수집 대상이
회차마다 순환해 주장이 사라졌다 돌아오는데, cascade 로 판정이 함께 지워지면
사람이 같은 글을 두 번 심사한다.

### 값의 임자를 가리는 규칙은 **둘**이다 — 하나로 합치면 반드시 틀린다

- **상호끼리는 왼쪽 우선.** '다라영어학원은 주 5회인데 가나수학학원은
  모르겠어요'에서 주 5회는 가나 쪽이 글자 수로 가깝지만 다라 이야기다.
  한국어는 수식어가 앞에 온다.
- **업종어('영어학원')는 더 가까울 때만 임자.** '주3회 2시간씩 영어학원을
  다녀요'의 영어학원은 수치 뒤에 있지만 그 수치의 임자다(다니다의 목적어).
  여기에 왼쪽 우선을 주면 '수학학원 알아보다가 가나수학학원 주 3회로
  정했어요' 같은 정상 근거가 날아간다.
- 거리만으로도, 왼쪽 우선만으로도 두 경우를 함께 못 잡는다.

한 겹 더: **문장에 남의 업종어만 있고 내 이름이 없으면 남의 값이다.**
제목의 일상어 '정상인가요'로 통과한 글의 본문 '주3회 2시간씩 영어학원을
다니고있어요'가 정상어학원의 수업 횟수가 됐다 — 그 문장에 정상은 없다.

### 실측이 드러낸 오탐 — 전부 회귀 테스트로 굳혔다

    '화,목 4시20분'          → 20분이 수업 길이가 됐다. 시각을 길이로 읽지 않는다
    '숙제 10분도 못 앉아'      → 숙제량이 됐다. **한국어는 부정어가 수치 뒤에 온다**
                              (감성의 NEGATORS 는 앞을 본다 — 반대다)
    '쿠킹스튜디오에서 한번 주문'  → '주 1회'가 됐다. 빗금을 요구한다(3회/주)
    '주1회 …할까 하는데 어떨까요?' → 사실이 됐다. 의문·전문은 사실이 아니다
    '대기실 비디오에서 보니'     → 대기자 명단이 됐다. 대기실은 방이다
    '숙제량 20분 · 10분~5시간'  → 중앙값 20분은 근거에 없는 정밀도다.
                              최댓값이 최솟값의 3배를 넘으면 값을 내지 않는다

**의문형 어미를 넓게 잡으면 안 된다.** `나요` 를 넣었더니 '숙제 10분이면
**끝나요**'가 의문문이 됐다 — 어간의 일부다. 물음표와 중의성 없는 표지만
쓴다. 한국어 질문글은 대개 물음표를 찍는다.

**레벨테스트는 만능 거부권이 아니다.** 창 안에 레테가 있으면 무조건
건너뛰게 했더니 '레테에서 떨어졌어요. 수업은 주 3회입니다'의 주 3회까지
사라졌다. 레벨테스트는 **가장 가까울 때만 이기는 하나의 주제**다.
('테스트'는 '레벨테스트'의 일부이므로 겹치는 자리는 세지 않는다 —
아니면 '레벨테스트는 월 1회'가 정기시험이 된다)

## 진입난이도는 낱말이 아니라 사건으로 잰다

옛 식을 캐시로 재 보니(언급 30건·정원 200) 셋이 틀려 있었다.

    레테 얘기가 아예 없는 학원        24.7   '모른다' 가 '쉽다' 로
    '난이도 높아 떨어졌어요'(경험담)   74.1   기준점
    '레테 어렵지 않았어요, 대기 없이'  70.6   부정문이 그대로 난이도
    '레테 어렵다던데 대기 걸리나요?'   74.1   질문글이 경험담과 동일

원인이 셋. ① `selectivity_signals` 가 창 없이 글 전체를 봤다 — 과목·학급은
이름 근처 45자만 보는데 **진입난이도만 그 규칙 밖에 있었다.** ② 감성에는
NEGATORS 가 걸리는데 여기엔 안 걸렸다. ③ 전문·의문을 사실과 구분하지
않았다. 배점 30%인 '정원 대비 수요'는 후기 내용이 아니라 `언급 수 ÷ 정원`
이라 **화제성과 같은 재료를 두 번** 썼다.

→ 다섯 사건 중 무엇이 확인됐는지를 묻는다. 무게가 다르다: 탈락 1.0 ·
  대기 0.8 · 마감 0.6 · 어렵게 통과 0.5 · 레테 있음 0.3.
  세 조건(근접 ±45자 · 부정 아님 · 전문·의문 아님)을 모두 통과해야 근거다.

    진입난이도 = 0.55 사건강도 + 0.25 확인율 + 0.20 최신성
    사건 0건이면 **점수를 내지 않는다**(None) → 남은 기둥으로 가중치 재정규화

**★ 강도는 종류별로 포화시킨다.** 처음에는 사건을 전부 더했더니 '레테
있음'(0.3)이 **양으로 쌓여** 소마·개념폴리아가 96~97점을 받았다 — 무게를
0.3 으로 둔 뜻이 통째로 상쇄됐다(실측 1,555건 중 대부분이 이 종류다).
종류마다 서로 다른 작성자 수로 포화시키면 무게가 살아난다.

**표본 수로 나누지 않는다.** 진입난이도는 '사건이 일어났는가'이지 '글 중
몇 %가 그 얘기를 하는가'가 아니다. 나누면 후기가 많은 큰 학원이 자동으로
쉬운 곳이 된다.

**가중치를 재정규화하면 합이 정확히 1이어야 한다.** 화면과 산식 페이지가
이 값을 그대로 찍는데 0.5385+0.2308+0.2308=1.0001 이면 공개한 산식이 틀린
것이 된다. 가장 무거운 기둥이 나머지를 흡수한다.

실측(캐시 재실행): 분포가 이전 n=272 · 18.7~54.3(눌려 있었다)에서
n=68 · 13.8~81.7 로 풀렸고 195곳은 점수가 없다. 등수 58곳 변동.
**실효 기여**(가중치×표준편차)를 다시 쟀다 — 평판 1.46→2.81(정보량 문턱
효과), 진입난이도 2.06→**3.87**(1위). 눌려 있던 값이 풀린 결과이므로
가중치를 다시 볼 만하다.

## 게이트는 배제와 등급을 나눈다 — 얕은 글은 버리지 않는다

'홍보글이나 정보가 제한적인 글을 제거'라는 요구에서 **둘은 결과가 달라야
한다.** 홍보글은 증거가 아니므로 배제한다. 얕은 글은 증거가 아닌 게 아니라
**어떤 종류의 증거가 아닐 뿐**이다.

    정보량 0  이름만 스침         화제성만
    정보량 1  의견 있음(감성어)    화제성 + 평판
    정보량 2  검증 가능한 사실     전부

얕은 글을 통째로 버리면 **화제성이 정보량을 재는 지표로 변질된다** — 한
줄짜리 '거기 좋대요'도 회자의 증거로는 유효하다. 안 버리면 감성 0.0 이
코호트 평균을 끌어당긴다. 버리는 대신 **쓸 수 있는 곳에서만** 쓴다.

홍보 신호 둘은 **글 하나가 아니라 묶음**을 봐야 잡힌다.
- 근사중복(2-gram Jaccard 0.9) — 가장 오래된 한 건만 남긴다. 원본까지
  버리면 근거가 통째로 사라진다.
- 작성자 버스트(7일 3건+) — `flag_repeat_authors` 에는 시간 창이 없어
  3년에 걸친 4건과 사흘 만의 4건이 같은 취급이었다.

**★ 버스트에 발견일(`date_source='discovery'`)을 쓰면 안 된다.** 한 회차에
발견한 글 수천 건이 **같은 날짜**를 갖는다. 이걸 빼먹었더니 3,256건이
버스트로 잡혔다 — 캠페인이 아니라 **우리 수집 일정**이었다.

## 취소는 삭제가 아니라 스위치다

`posts.py` 의 무효화가 글 전체를 끈다면 `claim_verdicts` 는 한 층 아래다 —
그 글의 평판 근거는 살리고 진입난이도 주장만 끈다.

    앱 '이의' → claim_disputes(접수만) → 운영자 판정 → claim_verdicts
    → 다음 실행이 원자료에서 전면 재빌드
    → 그 학원 점수 → 코호트 평균·표준편차 → 코호트 전원의 등수

매 실행 전부를 다시 지으므로 '어디까지 지워야 하나'를 사람이 판단할 것이
없고, **판정을 지우면 결정적 id 덕분에 같은 주장이 그대로 되살아난다.**

자동 반영은 하나뿐이다: 사유가 '지금은 다름'이고 그 주장이 2년 넘은 것이면
사람을 기다리지 않는다. 낡음은 가짜가 아니고 되돌리기도 쉽다. '사실 아님'은
언제나 사람이 본다 — 접수가 곧 편집권이 되면 그것도 왜곡이다.

**남용을 막는 셋.**
1. 취소는 점수를 **올리지 않는다.** 사건이 0건이 되면 점수는 올라가는 게
   아니라 사라진다(None).
2. 취소율을 상세 화면에 공개한다. 숨기면 그 자체가 왜곡이다.
3. 사유 없는 취소는 엔진이 무시한다(`posts.py` 와 같은 규칙).

취소된 주장은 목록에 남고 화면에 취소선과 사유가 붙는다. **그냥 사라지면
다음 사람이 같은 이의를 다시 제기한다.**

## LLM 은 값을 만들지 않고 문장을 가리킨다

'LLM 문장이 산식에 흘러들면 그때부터 산식을 설명할 수 없다'는 원칙은
그대로다. 사실 추출에만 LLM 을 허용하되 경계를 셋 세웠다.

1. **기둥 점수(평판·진입난이도)에는 안 쓴다.** 규칙 추출만. 산식은 결정적.
2. **인용문을 반드시 함께 뱉게 하고, 원문의 글자 그대로(띄어쓰기만 무시)가
   아니면 버린다**(`claim_llm.verify`). 기계가 검증할 수 있고 화면에도 그
   문장이 그대로 나간다. 지어낼 자리가 없다.
3. **사실 카드는 트리스코어에 들어가지 않는다.** 숙제가 많은 것은 좋은
   것도 나쁜 것도 아니다.

정의한 적 없는 종류를 모델이 만들어 내면 버린다 — 그 종류가 무엇을 뜻하는지
아무도 정한 적이 없다.

**주장 분해는 질문 생성보다 앞에 와야 한다.** 뒤에 두면 `questions` 가 이번
회차의 주장을 못 봐서 답이 한 회차 늦게 반영된다 — `apply_answers` 를
`load_rules` 앞에 둔 것과 같은 이유다.
